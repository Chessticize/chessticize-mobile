interface:
  display_name: "Chessticize Release Delta QA"
  short_description: "Summarize release changes or run requested visual QA"
  default_prompt: "Use $chessticize-release-delta-qa to handle the requested release comparison. Keep change summaries read-only; only execute simulator captures and the visual QA workflow when visual QA is requested. Report exact source identities and the limits of existing or newly collected evidence."
