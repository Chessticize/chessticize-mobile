interface:
  display_name: "Chessticize Local E2E"
  short_description: "Prepare and run exact-head iOS Detox locally"
  default_prompt: "Use $chessticize-mobile-local-e2e to prepare the iOS environment and run the exact-head local Detox gate."
