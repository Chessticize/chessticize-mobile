diff --git a/lib/commonjs/components/promotion-dialog.js b/lib/commonjs/components/promotion-dialog.js
index de72beac3b9cda50edb7a8f34b1630cfdd2bd8cf..36f900ad0525e8a0385464295e7ece6daf730988 100644
--- a/lib/commonjs/components/promotion-dialog.js
+++ b/lib/commonjs/components/promotion-dialog.js
@@ -13,28 +13,37 @@ function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e
 const PROMOTION_PIECES = ['q', 'r', 'b', 'n'];
+const PROMOTION_PIECE_NAMES = { q: 'queen', r: 'rook', b: 'bishop', n: 'knight' };
 const styles = _reactNative.StyleSheet.create({
   overlay: {
-    ..._reactNative.StyleSheet.absoluteFillObject,
-    backgroundColor: 'rgba(0, 0, 0, 0.5)',
+    flex: 1,
+    backgroundColor: 'rgba(15, 23, 42, 0.38)',
     justifyContent: 'center',
-    alignItems: 'center'
+    alignItems: 'center',
+    zIndex: 20
   },
   container: {
     flexDirection: 'row',
-    backgroundColor: 'white',
+    backgroundColor: '#F8FAFC',
     borderRadius: 8,
-    padding: 8,
+    borderColor: '#D8E0E8',
+    borderWidth: 1,
+    padding: 10,
+    zIndex: 1,
     shadowColor: '#000',
     shadowOffset: {
       width: 0,
-      height: 2
+      height: 8
     },
-    shadowOpacity: 0.25,
-    shadowRadius: 4,
+    shadowOpacity: 0.14,
+    shadowRadius: 18,
     elevation: 5
   },
   pieceButton: {
+    alignItems: 'center',
+    justifyContent: 'center',
     padding: 8,
-    borderRadius: 4,
+    borderRadius: 7,
+    borderColor: '#CBD5E1',
+    borderWidth: 1,
     marginHorizontal: 4
   },
   pieceImage: {
@@ -51,31 +59,38 @@ const PromotionDialog = exports.PromotionDialog = /*#__PURE__*/_react.default.me
   const {
     colors
   } = config;
+  const animatePresentation = _reactNative.Platform.OS !== 'android';
   return /*#__PURE__*/_react.default.createElement(_reactNative.Modal, {
     transparent: true,
     visible: true,
-    animationType: "fade",
+    animationType: animatePresentation ? "fade" : "none",
     onRequestClose: onCancel
-  }, /*#__PURE__*/_react.default.createElement(_reactNative.Pressable, {
+  }, /*#__PURE__*/_react.default.createElement(_reactNative.View, {
     style: styles.overlay,
+    testID: "promotion-dialog-overlay"
+  }, /*#__PURE__*/_react.default.createElement(_reactNative.Pressable, {
+    style: _reactNative.StyleSheet.absoluteFillObject,
     onPress: onCancel
-  }, /*#__PURE__*/_react.default.createElement(_reactNativeReanimated.default.View, {
-    entering: _reactNativeReanimated.FadeIn.duration(200),
-    exiting: _reactNativeReanimated.FadeOut.duration(200),
-    style: styles.container
+  }), /*#__PURE__*/_react.default.createElement(_reactNativeReanimated.default.View, {
+    entering: animatePresentation ? _reactNativeReanimated.FadeIn.duration(200) : undefined,
+    exiting: animatePresentation ? _reactNativeReanimated.FadeOut.duration(200) : undefined,
+    style: styles.container,
+    testID: "promotion-dialog-container"
   }, PROMOTION_PIECES.map(piece => {
     const pieceCode = `${color}${piece}`;
-    const source = _pieceImages.PIECE_SOURCES[pieceCode];
     return /*#__PURE__*/_react.default.createElement(_reactNative.TouchableOpacity, {
       key: piece,
       style: [styles.pieceButton, {
         backgroundColor: colors.promotionPieceButton
       }],
+      testID: `promotion-choice-${piece}`,
       onPress: () => onSelect(piece),
-      activeOpacity: 0.7
+      activeOpacity: 0.7,
+      accessibilityLabel: `Promote to ${PROMOTION_PIECE_NAMES[piece]}`
     }, /*#__PURE__*/_react.default.createElement(_reactNative.Image, {
-      source: source,
-      style: styles.pieceImage
+      source: _pieceImages.PIECE_SOURCES[pieceCode],
+      style: styles.pieceImage,
+      testID: `promotion-choice-${piece}-image`
     }));
   }))));
 });
diff --git a/lib/commonjs/components/skia/gesture-board.js b/lib/commonjs/components/skia/gesture-board.js
index 7ddc0516545a4e1e16774f520123aaa883c235c0..0c1eeb4b952cfea643b9ae9422a56809a34f512d 100644
--- a/lib/commonjs/components/skia/gesture-board.js
+++ b/lib/commonjs/components/skia/gesture-board.js
@@ -20,13 +20,17 @@ var _promotionDialog = require("../promotion-dialog");
 function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
 const styles = _reactNative.StyleSheet.create({
   container: {
-    aspectRatio: 1
+    aspectRatio: 1,
+    position: 'relative'
   }
 });
 const GestureBoard = exports.GestureBoard = /*#__PURE__*/(0, _react.forwardRef)(({
   onMove,
   onIllegalMove,
   renderEffect,
+  onReady,
+  blocksExternalGesture,
+  positionKey,
   spriteSource
 }, ref) => {
   const {
@@ -37,6 +41,21 @@ const GestureBoard = exports.GestureBoard = /*#__PURE__*/(0, _react.forwardRef)(
   const {
     image: spriteImage
   } = (0, _pieceImages.usePieceSpriteSheet)(spriteSource);
+  (0, _react.useEffect)(() => {
+    if (!spriteImage || !onReady) {
+      return;
+    }
+    let secondFrame = null;
+    const firstFrame = requestAnimationFrame(() => {
+      secondFrame = requestAnimationFrame(onReady);
+    });
+    return () => {
+      cancelAnimationFrame(firstFrame);
+      if (secondFrame !== null) {
+        cancelAnimationFrame(secondFrame);
+      }
+    };
+  }, [onReady, positionKey, spriteImage]);
 
   // Effect state for shader effects (all SharedValues for reactivity)
   const effectCenterX = (0, _reactNativeReanimated.useSharedValue)(0);
@@ -86,6 +105,12 @@ const GestureBoard = exports.GestureBoard = /*#__PURE__*/(0, _react.forwardRef)(
     promotionInfoRef.current = null;
     setShowPromotion(false);
   }, [boardState, config]);
+  const handleBoardReset = (0, _react.useCallback)(() => {
+    const info = promotionInfoRef.current;
+    info === null || info === void 0 || info.cancel();
+    promotionInfoRef.current = null;
+    setShowPromotion(false);
+  }, []);
 
   // Trigger effect on game events
   const triggerEffect = (0, _react.useCallback)((trigger, kingColor) => {
@@ -135,7 +160,8 @@ const GestureBoard = exports.GestureBoard = /*#__PURE__*/(0, _react.forwardRef)(
     chess,
     boardState,
     moveExecutor,
-    defaultHighlightColor: config.colors.lastMoveHighlight
+    defaultHighlightColor: config.colors.lastMoveHighlight,
+    onBeforeReset: handleBoardReset
   });
   const gesture = (0, _useBoardGesture.useBoardGesture)({
     boardState,
@@ -144,6 +170,7 @@ const GestureBoard = exports.GestureBoard = /*#__PURE__*/(0, _react.forwardRef)(
     // Lock the board while the promotion picker is open so a second drag
     // can't start (and re-trigger) another promotion.
     gestureEnabled: config.gestureEnabled && !showPromotion,
+    blocksExternalGesture,
     onIllegalMove
   });
   const containerStyle = (0, _react.useMemo)(() => [styles.container, {
diff --git a/lib/commonjs/components/skia/skia-dots.js b/lib/commonjs/components/skia/skia-dots.js
index 99152d0e193c3ff32b49cb57c78a5b09799a8617..27bd528cb50e65fefa778afe525945ca29584669 100644
--- a/lib/commonjs/components/skia/skia-dots.js
+++ b/lib/commonjs/components/skia/skia-dots.js
@@ -9,36 +9,58 @@ var _reactNativeSkia = require("@shopify/react-native-skia");
 var _reactNativeReanimated = require("react-native-reanimated");
 var _useBoardState = require("../../state/use-board-state");
 function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
-// All valid-move dots are built by ONE derived path that reads the shared
-// `validMoves` once (a single subscription) and adds a circle per target —
-// instead of 64 per-square components each subscribing to `validMoves` and
-// scanning it. It re-runs ONLY when `validMoves` changes (not every frame),
-// so it costs nothing at rest. Dots are binary (no per-square opacity), so
-// one Path is a perfect fit.
+// Valid-move markers are built by two derived paths that read the shared
+// `validMoves` once. Empty targets use quiet dots; capture targets use a ring
+// so the marker remains visible when a piece already occupies the target.
 const SkiaDots = exports.SkiaDots = /*#__PURE__*/_react.default.memo(({
   config,
   boardState
 }) => {
   const {
+    colors,
     pieceSize,
     flipped
   } = config;
   const radius = pieceSize * 0.15;
+  const captureRadius = pieceSize * 0.38;
   const half = pieceSize / 2;
-  const path = (0, _reactNativeReanimated.useDerivedValue)(() => {
-    const p = _reactNativeSkia.Skia.Path.Make();
+  const quietPath = (0, _reactNativeReanimated.useDerivedValue)(() => {
+    const path = _reactNativeSkia.Skia.Path.Make();
     const moves = boardState.validMoves.get();
     for (let i = 0; i < moves.length; i++) {
-      const pos = (0, _useBoardState.squareToPosition)(moves[i], pieceSize, flipped);
-      p.addCircle(pos.x + half, pos.y + half, radius);
+      const target = moves[i];
+      if (boardState.squares[target].piece.get()) {
+        continue;
+      }
+      const pos = (0, _useBoardState.squareToPosition)(target, pieceSize, flipped);
+      path.addCircle(pos.x + half, pos.y + half, radius);
     }
-    return p;
+    return path;
   });
-  return /*#__PURE__*/_react.default.createElement(_reactNativeSkia.Path, {
-    path: path,
-    color: "rgba(0, 0, 0, 0.3)",
-    opacity: 0.5
+  const capturePath = (0, _reactNativeReanimated.useDerivedValue)(() => {
+    const path = _reactNativeSkia.Skia.Path.Make();
+    const moves = boardState.validMoves.get();
+    for (let i = 0; i < moves.length; i++) {
+      const target = moves[i];
+      if (!boardState.squares[target].piece.get()) {
+        continue;
+      }
+      const pos = (0, _useBoardState.squareToPosition)(target, pieceSize, flipped);
+      path.addCircle(pos.x + half, pos.y + half, captureRadius);
+    }
+    return path;
   });
+  return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, /*#__PURE__*/_react.default.createElement(_reactNativeSkia.Path, {
+    path: quietPath,
+    color: colors.validMoveDot,
+    opacity: 0.5
+  }), /*#__PURE__*/_react.default.createElement(_reactNativeSkia.Path, {
+    path: capturePath,
+    color: colors.validMoveCapture,
+    opacity: 0.65,
+    style: "stroke",
+    strokeWidth: Math.max(2, pieceSize * 0.05)
+  }));
 });
 SkiaDots.displayName = 'SkiaDots';
 //# sourceMappingURL=skia-dots.js.map
\ No newline at end of file
diff --git a/lib/commonjs/components/skia/skia-pieces-atlas.js b/lib/commonjs/components/skia/skia-pieces-atlas.js
index d4a7146591891ed0ac8381633f06b668cb1b7ad0..83fc26beae5f0e14a951d3e88a55ac1ed98ba103 100644
--- a/lib/commonjs/components/skia/skia-pieces-atlas.js
+++ b/lib/commonjs/components/skia/skia-pieces-atlas.js
@@ -12,6 +12,7 @@ function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e
 // Sprite sheet layout: 6x2 grid (p, n, b, r, q, k for each color)
 // Row 0: white pieces, Row 1: black pieces
 const SPRITE_CELL_SIZE = 128;
+const MAX_PIECE_SLOTS = 32;
 
 // Pre-computed sprite rects for all piece types
 const SPRITE_RECTS = {
@@ -82,6 +83,14 @@ const SkiaPiecesAtlas = exports.SkiaPiecesAtlas = /*#__PURE__*/_react.default.me
       const scaledHalf = SPRITE_CELL_SIZE / 2 * pieceScale;
       transforms.push(_reactNativeSkia.Skia.RSXform(pieceScale, 0, centerX - scaledHalf, centerY - scaledHalf));
     }
+
+    // Atlas observes sprites and transforms through separate derived-value
+    // subscriptions. Keep both arrays at the board's fixed maximum piece
+    // count so an update cannot expose different lengths and crash Skia.
+    while (sprites.length < MAX_PIECE_SLOTS) {
+      sprites.push(SPRITE_RECTS.wp);
+      transforms.push(_reactNativeSkia.Skia.RSXform(0, 0, -pieceSize, -pieceSize));
+    }
     return {
       sprites,
       transforms
diff --git a/lib/commonjs/hooks/use-board-gesture.js b/lib/commonjs/hooks/use-board-gesture.js
index 26deda8bb43c022624db07d6ea463a1028f082f9..3f254c3319f5f012d0a48506936b13cb8aa1181d 100644
--- a/lib/commonjs/hooks/use-board-gesture.js
+++ b/lib/commonjs/hooks/use-board-gesture.js
@@ -14,12 +14,14 @@ const useBoardGesture = ({
   config,
   moveExecutor,
   gestureEnabled,
+  blocksExternalGesture,
   onIllegalMove
 }) => {
   const {
     pieceSize,
     animations,
-    flipped
+    flipped,
+    draggableColor
   } = config;
 
   // Track the currently dragged piece
@@ -29,11 +31,25 @@ const useBoardGesture = ({
   // Offset between touch point and piece position (to follow finger exactly)
   const touchOffsetX = (0, _reactNativeReanimated.useSharedValue)(0);
   const touchOffsetY = (0, _reactNativeReanimated.useSharedValue)(0);
+  // Square that was already selected when the current touch began. pan.onBegin
+  // pre-selects the touched piece on touch-down, so the tap handler needs this
+  // to tell a "toggle off" tap from the tap that created the selection.
+  const selectedAtTouchStart = (0, _reactNativeReanimated.useSharedValue)(null);
+  // True while a drag accepted by onStart is in flight. onEnd used to infer
+  // this from the square's zIndex, but a board reset (a fen-prop change
+  // landing mid-drag, e.g. right after an opponent-reply animation) zeroes
+  // zIndex, which made onEnd swallow the drop entirely — no tryMove, no
+  // onIllegalMove, and the sprite was left at the drop point.
+  const dragActivated = (0, _reactNativeReanimated.useSharedValue)(false);
 
   // Stable callback for tryMove
-  const handleTryMove = (0, _react.useCallback)((from, to) => {
-    moveExecutor.tryMove(from, to);
-  }, [moveExecutor]);
+  const handleTryMove = (0, _react.useCallback)((from, to, notifyOnInvalid = false) => {
+    void moveExecutor.tryMove(from, to).then(move => {
+      if (!move && notifyOnInvalid) {
+        onIllegalMove === null || onIllegalMove === void 0 || onIllegalMove(from, to);
+      }
+    });
+  }, [moveExecutor, onIllegalMove]);
 
   // Stable callback for selectPiece
   const handleSelectPiece = (0, _react.useCallback)(square => {
@@ -55,17 +71,32 @@ const useBoardGesture = ({
         y
       } = event;
       const square = (0, _useBoardState.positionToSquare)(x, y, pieceSize, flipped);
+      selectedAtTouchStart.set(boardState.selectedSquare.get());
       const squareState = boardState.squares[square];
       const piece = squareState.piece.get();
-      if (piece) {
-        // Store drag start info (but don't start drag yet)
-        draggedSquare.set(square);
-        dragStartX.set(squareState.translateX.get());
-        dragStartY.set(squareState.translateY.get());
-        const pieceCenterX = squareState.translateX.get() + pieceSize / 2;
-        const pieceCenterY = squareState.translateY.get() + pieceSize / 2;
-        touchOffsetX.set(x - pieceCenterX);
-        touchOffsetY.set(y - pieceCenterY);
+      const turn = boardState.turn.get();
+      const allowedDragColor = draggableColor ?? turn;
+      if (!piece || piece[0] !== allowedDragColor) {
+        draggedSquare.set(null);
+        return;
+      }
+
+      // Store drag start info (but don't start drag yet)
+      draggedSquare.set(square);
+      dragStartX.set(squareState.translateX.get());
+      dragStartY.set(squareState.translateY.get());
+      const pieceCenterX = squareState.translateX.get() + pieceSize / 2;
+      const pieceCenterY = squareState.translateY.get() + pieceSize / 2;
+      touchOffsetX.set(x - pieceCenterX);
+      touchOffsetY.set(y - pieceCenterY);
+
+      // Start the valid-target calculation before the drag crosses the
+      // min-distance threshold. Otherwise very fast drags can reach onEnd
+      // before the RN-thread select callback has populated validMoves.
+      // Skip when the square is already selected: validMoves are populated,
+      // and re-selecting could race a deselect issued by the tap handler.
+      if (boardState.selectedSquare.get() !== square) {
+        (0, _reactNativeWorklets.scheduleOnRN)(handleSelectPiece, square);
       }
     }).onStart(() => {
       'worklet';
@@ -76,16 +107,23 @@ const useBoardGesture = ({
       const squareState = boardState.squares[square];
       const piece = squareState.piece.get();
       const turn = boardState.turn.get();
+      const allowedDragColor = draggableColor ?? turn;
+      const isOwnPiece = piece && piece[0] === allowedDragColor;
+
+      // Do not start dragging opponent pieces; otherwise onUpdate can visibly
+      // move them until onEnd snaps them back.
+      if (!isOwnPiece) {
+        draggedSquare.set(null);
+        return;
+      }
 
       // Raise and scale the piece
+      dragActivated.set(true);
       squareState.zIndex.set(100);
       squareState.scale.set((0, _reactNativeReanimated.withSpring)(1.1, animations.scale));
 
       // Only show valid moves (dots) for own pieces
-      const isOwnPiece = piece && piece[0] === turn;
-      if (isOwnPiece) {
-        (0, _reactNativeWorklets.scheduleOnRN)(handleSelectPiece, square);
-      }
+      (0, _reactNativeWorklets.scheduleOnRN)(handleSelectPiece, square);
     }).onUpdate(event => {
       'worklet';
 
@@ -97,25 +135,45 @@ const useBoardGesture = ({
       // Subtracting pieceSize/2 converts from center back to top-left (translateX/Y)
       squareState.translateX.set(event.x - touchOffsetX.get() - pieceSize / 2);
       squareState.translateY.set(event.y - touchOffsetY.get() - pieceSize / 2);
-    }).onEnd(() => {
+    }).onEnd((_event, success) => {
       'worklet';
 
       const square = draggedSquare.get();
       if (!square) return;
       const squareState = boardState.squares[square];
 
-      // Only process drop if piece was actually dragged (zIndex > 0)
-      const wasDragged = squareState.zIndex.get() > 0;
+      // Only process drop if the drag actually activated (onStart accepted
+      // it). Tracked in a gesture-local flag rather than the square's zIndex:
+      // board resets can land mid-drag, wipe zIndex, and must not swallow
+      // the drop.
+      const wasDragged = dragActivated.get();
+      dragActivated.set(false);
       if (!wasDragged) {
         draggedSquare.set(null);
         return;
       }
       squareState.scale.set((0, _reactNativeReanimated.withSpring)(1, animations.scale));
 
+      // The gesture was cancelled mid-drag (success === false: e.g. a board
+      // lock flipped `enabled` while the finger was down). Never derive a
+      // drop from wherever the cancellation caught the piece — snap it home
+      // and clear the selection like a failed drop would.
+      if (!success) {
+        const originalPos = (0, _useBoardState.squareToPosition)(square, pieceSize, flipped);
+        squareState.translateX.set((0, _reactNativeReanimated.withSpring)(originalPos.x, animations.snapBack));
+        squareState.translateY.set((0, _reactNativeReanimated.withSpring)(originalPos.y, animations.snapBack));
+        squareState.zIndex.set(0);
+        boardState.selectedSquare.set(null);
+        boardState.validMoves.set([]);
+        draggedSquare.set(null);
+        return;
+      }
+
       // Check if this is own piece - only own pieces can make moves
       const piece = squareState.piece.get();
       const turn = boardState.turn.get();
-      const isOwnPiece = piece && piece[0] === turn;
+      const allowedDragColor = draggableColor ?? turn;
+      const isOwnPiece = piece && piece[0] === allowedDragColor;
 
       // If not own piece, just snap back (can't make moves with opponent's pieces)
       if (!isOwnPiece) {
@@ -147,6 +205,17 @@ const useBoardGesture = ({
         draggedSquare.set(null);
         return;
       }
+      if (targetSquare !== square && validMoves.length === 0) {
+        const originalPos = (0, _useBoardState.squareToPosition)(square, pieceSize, flipped);
+        squareState.translateX.set(originalPos.x);
+        squareState.translateY.set(originalPos.y);
+        squareState.zIndex.set(0);
+        boardState.selectedSquare.set(null);
+        boardState.validMoves.set([]);
+        (0, _reactNativeWorklets.scheduleOnRN)(handleTryMove, square, targetSquare, true);
+        draggedSquare.set(null);
+        return;
+      }
 
       // Invalid move - snap back to original position and clear selection
       const originalPos = (0, _useBoardState.squareToPosition)(square, pieceSize, flipped);
@@ -172,6 +241,7 @@ const useBoardGesture = ({
           squareState.zIndex.set(0);
         }
       }
+      dragActivated.set(false);
       draggedSquare.set(null);
     });
 
@@ -187,20 +257,30 @@ const useBoardGesture = ({
       const selectedSquare = boardState.selectedSquare.get();
       const piece = boardState.squares[square].piece.get();
       const turn = boardState.turn.get();
-      const isOwnPiece = piece && piece[0] === turn;
+      const allowedDragColor = draggableColor ?? turn;
+      const isOwnPiece = piece && piece[0] === allowedDragColor;
 
       // Case 1: No piece selected - try to select own piece
       if (!selectedSquare) {
         if (isOwnPiece) {
+          boardState.selectedSquare.set(square);
+          boardState.validMoves.set([]);
           (0, _reactNativeWorklets.scheduleOnRN)(handleSelectPiece, square);
         }
         return;
       }
 
-      // Case 2: Tapped on same piece - deselect
+      // Case 2: Tapped on same piece - deselect, but only when the piece
+      // was already selected before this touch began. pan.onBegin selects
+      // the touched piece on touch-down, so without this guard every quick
+      // tap would read its own selection here and immediately toggle it
+      // off (dots flash and vanish; only >maxDuration presses survived
+      // because the failed tap never ran onEnd).
       if (square === selectedSquare) {
-        boardState.selectedSquare.set(null);
-        boardState.validMoves.set([]);
+        if (selectedAtTouchStart.get() === square) {
+          boardState.selectedSquare.set(null);
+          boardState.validMoves.set([]);
+        }
         return;
       }
 
@@ -213,18 +293,38 @@ const useBoardGesture = ({
 
       // Case 4: Tapped on another own piece - switch selection
       if (isOwnPiece) {
+        boardState.selectedSquare.set(square);
+        boardState.validMoves.set([]);
         (0, _reactNativeWorklets.scheduleOnRN)(handleSelectPiece, square);
         return;
       }
 
-      // Case 5: Invalid target - deselect
+      // Case 5: Valid moves may not have crossed from the RN thread to the
+      // UI worklet yet after a fast source tap. Let the executor make the
+      // final legality decision so tap-tap stays responsive.
+      if (validMoves.length === 0) {
+        boardState.selectedSquare.set(null);
+        boardState.validMoves.set([]);
+        (0, _reactNativeWorklets.scheduleOnRN)(handleTryMove, selectedSquare, square, true);
+        return;
+      }
+
+      // Case 6: Invalid target - deselect
       boardState.selectedSquare.set(null);
       boardState.validMoves.set([]);
     });
 
-    // Combine gestures - pan takes precedence
-    return _reactNativeGestureHandler.Gesture.Simultaneous(tapGesture, panGesture);
-  }, [boardState, pieceSize, flipped, draggedSquare, dragStartX, dragStartY, touchOffsetX, touchOffsetY, animations, gestureEnabled, handleTryMove, handleSelectPiece, handleIllegalMove]);
+    // Relations attached to a composed Race are not propagated to its raw
+    // gestures. Block the parent scroll from both candidates before composing.
+    if (blocksExternalGesture) {
+      tapGesture.blocksExternalGesture(blocksExternalGesture);
+      panGesture.blocksExternalGesture(blocksExternalGesture);
+    }
+
+    // Tap should not wait for the pan candidate to settle. A real drag still
+    // wins once it crosses minDistance before the tap can activate on release.
+    return _reactNativeGestureHandler.Gesture.Race(tapGesture, panGesture);
+  }, [boardState, pieceSize, flipped, draggedSquare, dragStartX, dragStartY, touchOffsetX, touchOffsetY, selectedAtTouchStart, dragActivated, animations, gestureEnabled, blocksExternalGesture, draggableColor, handleTryMove, handleSelectPiece, handleIllegalMove]);
   return gesture;
 };
 exports.useBoardGesture = useBoardGesture;
diff --git a/lib/commonjs/hooks/use-chessboard-ref.js b/lib/commonjs/hooks/use-chessboard-ref.js
index 759528090f2fb7b1e3ddc0bdc47841ceea24c1ab..5a2285f370efed55ab7c50f303161db1735060f1 100644
--- a/lib/commonjs/hooks/use-chessboard-ref.js
+++ b/lib/commonjs/hooks/use-chessboard-ref.js
@@ -12,7 +12,8 @@ const useChessboardRef = ({
   chess,
   boardState,
   moveExecutor,
-  defaultHighlightColor = 'rgba(255, 255, 0, 0.5)'
+  defaultHighlightColor = 'rgba(255, 255, 0, 0.5)',
+  onBeforeReset
 }) => {
   const move = (0, _react.useCallback)(async params => {
     return moveExecutor.tryMove(params.from, params.to, params.promotion);
@@ -36,8 +37,9 @@ const useChessboardRef = ({
     boardState.kingInCheckSquare.set(null);
   }, [boardState.highlights, boardState.squares, boardState.lastMove, boardState.kingInCheckSquare]);
   const resetBoard = (0, _react.useCallback)((fen, opts) => {
-    moveExecutor.resetBoard(fen, opts);
-  }, [moveExecutor]);
+    onBeforeReset === null || onBeforeReset === void 0 || onBeforeReset();
+    return moveExecutor.resetBoard(fen, opts);
+  }, [moveExecutor, onBeforeReset]);
   const getState = (0, _react.useCallback)(() => {
     return (0, _getChessboardState.getChessboardState)(chess);
   }, [chess]);
diff --git a/lib/commonjs/index.js b/lib/commonjs/index.js
index 360d75d327324795ca72a43b37965b1ea2bc19d9..b72d8157424094f16b0d79a66fa6228180328b85 100644
--- a/lib/commonjs/index.js
+++ b/lib/commonjs/index.js
@@ -12,6 +12,7 @@ const Chessboard = exports.Chessboard = /*#__PURE__*/(0, _react.forwardRef)(({
   fen,
   boardSize,
   gestureEnabled,
+  draggableColor,
   flipped,
   withLetters,
   withNumbers,
@@ -20,6 +21,8 @@ const Chessboard = exports.Chessboard = /*#__PURE__*/(0, _react.forwardRef)(({
   onMove,
   onIllegalMove,
   renderEffect,
+  onReady,
+  blocksExternalGesture,
   spriteSource,
   fontSource
 }, ref) => {
@@ -27,6 +30,7 @@ const Chessboard = exports.Chessboard = /*#__PURE__*/(0, _react.forwardRef)(({
     fen: fen,
     boardSize: boardSize,
     gestureEnabled: gestureEnabled,
+    draggableColor: draggableColor,
     flipped: flipped,
     withLetters: withLetters,
     withNumbers: withNumbers,
@@ -38,6 +42,9 @@ const Chessboard = exports.Chessboard = /*#__PURE__*/(0, _react.forwardRef)(({
     onMove: onMove,
     onIllegalMove: onIllegalMove,
     renderEffect: renderEffect,
+    onReady: onReady,
+    blocksExternalGesture: blocksExternalGesture,
+    positionKey: fen,
     spriteSource: spriteSource
   }));
 });
diff --git a/lib/commonjs/state/board-state-context.js b/lib/commonjs/state/board-state-context.js
index cfd52d5394e32d3972be93c85977c62f4ad30885..297972ef10bbda6a19b00c88166ac3a916364387 100644
--- a/lib/commonjs/state/board-state-context.js
+++ b/lib/commonjs/state/board-state-context.js
@@ -19,7 +19,9 @@ const defaultColors = {
   black: '#62B1A8',
   lastMoveHighlight: 'rgba(255,255,0, 0.5)',
   checkmateHighlight: '#E84855',
-  promotionPieceButton: '#FF9B71'
+  promotionPieceButton: '#FF9B71',
+  validMoveDot: 'rgba(0, 0, 0, 0.3)',
+  validMoveCapture: 'rgba(0, 0, 0, 0.55)'
 };
 const defaultDurations = {
   move: 150
@@ -34,6 +36,7 @@ const BoardStateProvider = ({
   fen,
   boardSize = DEFAULT_BOARD_SIZE,
   gestureEnabled = true,
+  draggableColor = null,
   flipped = false,
   withLetters = true,
   withNumbers = true,
@@ -46,6 +49,7 @@ const BoardStateProvider = ({
     boardSize,
     pieceSize,
     gestureEnabled,
+    draggableColor,
     flipped,
     withLetters,
     withNumbers,
@@ -59,7 +63,7 @@ const BoardStateProvider = ({
     },
     animations: defaultAnimations,
     fontSource: fontSource ?? null
-  }), [boardSize, pieceSize, gestureEnabled, flipped, withLetters, withNumbers, colors, durations, fontSource]);
+  }), [boardSize, pieceSize, gestureEnabled, draggableColor, flipped, withLetters, withNumbers, colors, durations, fontSource]);
   const {
     boardState,
     chess
diff --git a/lib/commonjs/state/move-executor.js b/lib/commonjs/state/move-executor.js
index 983112b3bfd62a93c150077851c9cdc0e6fc5c48..57fccec6ee15028cd1d4bcd74e158734eeb05559 100644
--- a/lib/commonjs/state/move-executor.js
+++ b/lib/commonjs/state/move-executor.js
@@ -61,51 +61,56 @@ const createMoveExecutor = (chess, boardState, config, callbacks) => {
     if (!move) return null;
     const fromState = boardState.squares[from];
     const toState = boardState.squares[to];
-    const movingPiece = fromState.piece.get();
-
-    // Handle capture - clear target square piece
-    if (move.captured) {
-      toState.piece.set(null);
-    }
 
     // Animate the piece
     const toPos = (0, _useBoardState.squareToPosition)(to, pieceSize, flipped);
     const fromPos = (0, _useBoardState.squareToPosition)(from, pieceSize, flipped);
 
-    // Raise the moving piece
-    fromState.zIndex.set(100);
+    // Pre-compute the final piece code to avoid capturing complex objects in
+    // worklet. Derived from the chess move (not the origin sprite slot) so a
+    // sprite-level desync can never plant a null piece on the destination.
+    const finalPieceCode = promotionPiece ? `${move.color}${promotionPiece}` : `${move.color}${move.piece}`;
 
-    // Pre-compute the final piece code to avoid capturing complex objects in worklet
-    const finalPieceCode = promotionPiece ? `${move.color}${promotionPiece}` : movingPiece;
+    // Migrate the piece to its destination slot immediately, so the sprite
+    // slots always match the chess position (chess.move already ran above).
+    // Parking the piece in the origin slot until the glide settled — the old
+    // flow — made a fast re-grab of a just-moved piece find an empty
+    // destination square, and any interruption of the glide (board reset,
+    // premove replay) left the sprite stranded on the wrong slot. The glide
+    // renders from the destination slot instead, starting wherever the piece
+    // currently is: the origin square for tap moves, or the drop point for
+    // drags. Setting the destination piece also consumes any captured piece.
+    const startX = fromState.translateX.get();
+    const startY = fromState.translateY.get();
+    fromState.piece.set(null);
+    fromState.translateX.set(fromPos.x);
+    fromState.translateY.set(fromPos.y);
+    fromState.zIndex.set(0);
+    toState.piece.set(finalPieceCode);
+    toState.translateX.set(startX);
+    toState.translateY.set(startY);
+    toState.zIndex.set(100);
     const commitMove = finished => {
       'worklet';
 
-      // Only commit the sprite writes if the spring actually settled. A
-      // cancelled spring (resetBoard, or a newer animation on the same
-      // square) means whoever cancelled it now owns this square's state —
-      // writing here would smear stale pieces onto the fresh board.
+      // The slots migrated at move time; settling only lowers the piece. A
+      // cancelled glide (resetBoard, a re-grab, or a newer animation on the
+      // same square) means whoever cancelled it owns the square's state now.
       if (finished) {
-        // Move complete - update piece positions
-        toState.piece.set(finalPieceCode);
-        fromState.piece.set(null);
-
-        // Reset position to original square for future use
-        fromState.translateX.set(fromPos.x);
-        fromState.translateY.set(fromPos.y);
-        fromState.zIndex.set(0);
+        toState.zIndex.set(0);
       }
       if (onAnimationComplete) {
         (0, _reactNativeWorklets.scheduleOnRN)(onAnimationComplete);
       }
     };
 
-    // Attach the commit to an axis that actually travels. A spring whose
-    // target equals its current value settles immediately — putting the
-    // callback there commits the move before any motion happens, so the
-    // piece teleports (horizontal moves have dy === 0).
-    const movesVertically = toPos.y !== fromPos.y;
-    fromState.translateX.set((0, _reactNativeReanimated.withSpring)(toPos.x, animations.move, movesVertically ? undefined : commitMove));
-    fromState.translateY.set((0, _reactNativeReanimated.withSpring)(toPos.y, animations.move, movesVertically ? commitMove : undefined));
+    // Attach the commit to the axis with the larger travel. A spring whose
+    // target equals its current value settles immediately — riding a
+    // no-travel axis would fire the commit (and resolve tryMove) before any
+    // motion happens.
+    const movesVertically = Math.abs(toPos.y - startY) >= Math.abs(toPos.x - startX);
+    toState.translateX.set((0, _reactNativeReanimated.withSpring)(toPos.x, animations.move, movesVertically ? undefined : commitMove));
+    toState.translateY.set((0, _reactNativeReanimated.withSpring)(toPos.y, animations.move, movesVertically ? commitMove : undefined));
 
     // Handle castling - move the rook
     if (move.flags.includes('k') || move.flags.includes('q')) {
@@ -115,22 +120,22 @@ const createMoveExecutor = (chess, boardState, config, callbacks) => {
       const rookTo = (isKingside ? 'f' : 'd') + rank;
       const rookFromState = boardState.squares[rookFrom];
       const rookToState = boardState.squares[rookTo];
-      const rookPiece = rookFromState.piece.get();
+      const rookPiece = `${move.color}r`;
       const rookToPos = (0, _useBoardState.squareToPosition)(rookTo, pieceSize, flipped);
       const rookFromPos = (0, _useBoardState.squareToPosition)(rookFrom, pieceSize, flipped);
 
-      // The rook slides along its rank — horizontal — so the commit must
-      // ride the X spring (the Y spring settles instantly; see commitMove).
-      rookFromState.translateX.set((0, _reactNativeReanimated.withSpring)(rookToPos.x, animations.move, finished => {
-        'worklet';
-
-        if (!finished) return;
-        rookToState.piece.set(rookPiece);
-        rookFromState.piece.set(null);
-        rookFromState.translateX.set(rookFromPos.x);
-        rookFromState.translateY.set(rookFromPos.y);
-      }));
-      rookFromState.translateY.set((0, _reactNativeReanimated.withSpring)(rookToPos.y, animations.move));
+      // Same immediate slot migration as the moving piece: the rook lives in
+      // its destination slot from move time and glides in from its origin.
+      const rookStartX = rookFromState.translateX.get();
+      const rookStartY = rookFromState.translateY.get();
+      rookFromState.piece.set(null);
+      rookFromState.translateX.set(rookFromPos.x);
+      rookFromState.translateY.set(rookFromPos.y);
+      rookToState.piece.set(rookPiece);
+      rookToState.translateX.set(rookStartX);
+      rookToState.translateY.set(rookStartY);
+      rookToState.translateX.set((0, _reactNativeReanimated.withSpring)(rookToPos.x, animations.move));
+      rookToState.translateY.set((0, _reactNativeReanimated.withSpring)(rookToPos.y, animations.move));
     }
 
     // Handle en passant - remove the captured pawn
@@ -240,82 +245,92 @@ const createMoveExecutor = (chess, boardState, config, callbacks) => {
       } catch {
         // Invalid FEN — leave the chess instance untouched and bail out so
         // the board state stays consistent with what's actually on screen.
-        return;
+        return Promise.resolve();
       }
     } else {
       chess.reset();
     }
     const slide = opts === null || opts === void 0 ? void 0 : opts.slide;
     const lastMove = (opts === null || opts === void 0 ? void 0 : opts.lastMove) ?? null;
+    const targetFlipped = (opts === null || opts === void 0 ? void 0 : opts.flipped) ?? flipped;
     const board = chess.board();
-
-    // Update every square. When `slide` is given, the piece landing on
-    // `slide.to` starts at `slide.from` and springs home — so stepping through
-    // a game's history animates the moved piece instead of snapping.
+    const squareSnapshots = [];
     for (let row = 0; row < 8; row++) {
       for (let col = 0; col < 8; col++) {
         const colChar = String.fromCharCode('a'.charCodeAt(0) + col);
         const rowNum = 8 - row;
         const square = `${colChar}${rowNum}`;
-        const sq = boardState.squares[square];
         const piece = board[row][col];
-        sq.piece.set(piece ? `${piece.color}${piece.type}` : null);
-        sq.scale.set(1);
-        sq.lastMove.set(!!lastMove && (square === lastMove.from || square === lastMove.to));
-        sq.inCheck.set(false);
-        const pos = (0, _useBoardState.squareToPosition)(square, pieceSize, flipped);
-        if (slide && square === slide.to) {
-          const fromPos = (0, _useBoardState.squareToPosition)(slide.from, pieceSize, flipped);
-          sq.zIndex.set(100);
-          sq.translateX.set(fromPos.x);
-          sq.translateY.set(fromPos.y);
-          // zIndex drop rides the axis that actually travels (a no-travel
-          // spring settles instantly — see commitMove in executeMove).
-          const slidesVertically = pos.y !== fromPos.y;
-          const dropZIndex = () => {
-            'worklet';
-
-            sq.zIndex.set(0);
-          };
-          sq.translateX.set((0, _reactNativeReanimated.withSpring)(pos.x, animations.move, slidesVertically ? undefined : dropZIndex));
-          sq.translateY.set((0, _reactNativeReanimated.withSpring)(pos.y, animations.move, slidesVertically ? dropZIndex : undefined));
-        } else {
-          sq.translateX.set(pos.x);
-          sq.translateY.set(pos.y);
-          sq.zIndex.set(0);
-        }
+        const pos = (0, _useBoardState.squareToPosition)(square, pieceSize, targetFlipped);
+        squareSnapshots.push({
+          square,
+          piece: piece ? `${piece.color}${piece.type}` : null,
+          x: pos.x,
+          y: pos.y
+        });
       }
     }
-
-    // Reset other state
-    boardState.turn.set(chess.turn());
-    boardState.selectedSquare.set(null);
-    boardState.validMoves.set([]);
-    boardState.lastMove.set(lastMove);
-
-    // Check / checkmate highlight for the resulting position.
+    const turn = chess.turn();
     const isInCheck = chess.isCheck();
-    const kingSquare = isInCheck || chess.isCheckmate() ? (0, _findKingSquare.findKingSquare)(chess, chess.turn()) : null;
-    if (kingSquare) {
-      boardState.squares[kingSquare].inCheck.set(true);
-    }
-    boardState.isCheck.set(isInCheck);
-    boardState.kingInCheckSquare.set(kingSquare);
+    const kingSquare = isInCheck || chess.isCheckmate() ? (0, _findKingSquare.findKingSquare)(chess, turn) : null;
+    const slideFromPosition = slide ? (0, _useBoardState.squareToPosition)(slide.from, pieceSize, targetFlipped) : null;
 
-    // Clear highlights
-    for (const square of Object.keys(boardState.highlights)) {
-      boardState.highlights[square].color.set(null);
-    }
+    // A reset is one visual position change. Run every SharedValue write in a
+    // single UI task so Android cannot paint a partly rewritten board between
+    // individual JS-to-UI writes. `flipped` lets a caller publish the next FEN
+    // in its target orientation before React reveals the next puzzle.
+    return new Promise(resolve => {
+      (0, _reactNativeWorklets.scheduleOnUI)((state, snapshots, nextTurn, nextLastMove, nextKingSquare, nextIsInCheck, nextSlide, nextSlideFromPosition, moveAnimation, effects, onCommitted) => {
+        'worklet';
 
-    // Reset shader effect SharedValues so a fresh game's first check or
-    // checkmate doesn't trigger a ripple at the previous game's king
-    // square (centerX/centerY were last written by triggerEffect).
-    if (callbacks.effectSharedValues) {
-      callbacks.effectSharedValues.centerX.set(0);
-      callbacks.effectSharedValues.centerY.set(0);
-      callbacks.effectSharedValues.progress.set(0);
-      callbacks.effectSharedValues.trigger.set('');
-    }
+        for (const snapshot of snapshots) {
+          const sq = state.squares[snapshot.square];
+          sq.piece.set(snapshot.piece);
+          sq.scale.set(1);
+          sq.lastMove.set(!!nextLastMove && (snapshot.square === nextLastMove.from || snapshot.square === nextLastMove.to));
+          sq.inCheck.set(snapshot.square === nextKingSquare);
+          if (nextSlide && snapshot.square === nextSlide.to) {
+            const fromPos = nextSlideFromPosition;
+            sq.zIndex.set(100);
+            sq.translateX.set(fromPos.x);
+            sq.translateY.set(fromPos.y);
+            // zIndex drop rides the axis that actually travels (a no-travel
+            // spring settles instantly — see commitMove in executeMove).
+            const slidesVertically = snapshot.y !== fromPos.y;
+            const dropZIndex = () => {
+              'worklet';
+
+              sq.zIndex.set(0);
+            };
+            const timing = nextSlide.durationMs === undefined ? null : {
+              duration: nextSlide.durationMs
+            };
+            sq.translateX.set(timing ? (0, _reactNativeReanimated.withTiming)(snapshot.x, timing, slidesVertically ? undefined : dropZIndex) : (0, _reactNativeReanimated.withSpring)(snapshot.x, moveAnimation, slidesVertically ? undefined : dropZIndex));
+            sq.translateY.set(timing ? (0, _reactNativeReanimated.withTiming)(snapshot.y, timing, slidesVertically ? dropZIndex : undefined) : (0, _reactNativeReanimated.withSpring)(snapshot.y, moveAnimation, slidesVertically ? dropZIndex : undefined));
+          } else {
+            sq.translateX.set(snapshot.x);
+            sq.translateY.set(snapshot.y);
+            sq.zIndex.set(0);
+          }
+        }
+        state.turn.set(nextTurn);
+        state.selectedSquare.set(null);
+        state.validMoves.set([]);
+        state.lastMove.set(nextLastMove);
+        state.isCheck.set(nextIsInCheck);
+        state.kingInCheckSquare.set(nextKingSquare);
+        for (const square of Object.keys(state.highlights)) {
+          state.highlights[square].color.set(null);
+        }
+        if (effects) {
+          effects.centerX.set(0);
+          effects.centerY.set(0);
+          effects.progress.set(0);
+          effects.trigger.set('');
+        }
+        (0, _reactNativeWorklets.scheduleOnRN)(onCommitted);
+      }, boardState, squareSnapshots, turn, lastMove, kingSquare, isInCheck, slide, slideFromPosition, animations.move, callbacks.effectSharedValues, resolve);
+    });
   };
   const undo = () => {
     const move = chess.undo();
diff --git a/lib/commonjs/state/use-board-state.js b/lib/commonjs/state/use-board-state.js
index 0a6faf6b1ec0b5ac518104d513f3e1a0a9c1a9b6..4a9ba5a255d44f8f9a140019f6f351a2263f57c8 100644
--- a/lib/commonjs/state/use-board-state.js
+++ b/lib/commonjs/state/use-board-state.js
@@ -125,8 +125,11 @@ const useBoardState = (initialFen, pieceSize, flipped = false) => {
   // squareStates were created once with the *initial* flipped value, so toggling
   // `flipped` at runtime would otherwise leave un-moved pieces stuck at their
   // original positions. A flip/resize is an orientation/scale change, not a
-  // move, so snap every occupied square to its new logical position instantly
-  // — springing them would make all pieces fly across the board at once.
+  // move, so snap every logical square slot to its new position instantly —
+  // springing them would make all pieces fly across the board at once. Do not
+  // consult the piece SharedValue here: resetBoard writes can still be queued
+  // on Android when this effect runs, so its current value may describe the
+  // previous FEN and cause newly occupied squares to keep the old orientation.
   const isFirstLayoutRef = (0, _react.useRef)(true);
   (0, _react.useEffect)(() => {
     if (isFirstLayoutRef.current) {
@@ -135,7 +138,6 @@ const useBoardState = (initialFen, pieceSize, flipped = false) => {
     }
     for (const square of _types.SQUARES) {
       const state = squareStates[square];
-      if (state.piece.get() === null) continue;
       const pos = squareToPosition(square, pieceSize, flipped);
       state.translateX.set(pos.x);
       state.translateY.set(pos.y);
@@ -155,6 +157,14 @@ const useBoardState = (initialFen, pieceSize, flipped = false) => {
       isFirstFenRef.current = false;
       return;
     }
+    // The screen aligns the fen prop with a position the board already
+    // reached through animated moves (opponent replies, premove replays).
+    // When the position already matches, the rewrite below would be a no-op
+    // for the pieces but would still wipe live gesture state — selection,
+    // valid-move dots, and an in-flight drag (zIndex) — mid-gesture. Skip it.
+    if (initialFen && chess.fen() === initialFen) {
+      return;
+    }
     if (!initialFen) {
       chess.reset();
     } else {
diff --git a/lib/module/components/promotion-dialog.js b/lib/module/components/promotion-dialog.js
index 5bf64b655079b16368383257bc53bc3bce4d8418..14f6c9229a38e371517b7655e11f4c58ffd5df38 100644
--- a/lib/module/components/promotion-dialog.js
+++ b/lib/module/components/promotion-dialog.js
@@ -1,32 +1,41 @@
 import React from 'react';
-import { StyleSheet, TouchableOpacity, Image, Modal, Pressable } from 'react-native';
+import { StyleSheet, TouchableOpacity, Image, Modal, Platform, Pressable, View } from 'react-native';
 import Animated, { FadeIn, FadeOut } from 'react-native-reanimated';
 import { PIECE_SOURCES } from '../assets/piece-images';
 const PROMOTION_PIECES = ['q', 'r', 'b', 'n'];
+const PROMOTION_PIECE_NAMES = { q: 'queen', r: 'rook', b: 'bishop', n: 'knight' };
 const styles = StyleSheet.create({
   overlay: {
-    ...StyleSheet.absoluteFillObject,
-    backgroundColor: 'rgba(0, 0, 0, 0.5)',
+    flex: 1,
+    backgroundColor: 'rgba(15, 23, 42, 0.38)',
     justifyContent: 'center',
-    alignItems: 'center'
+    alignItems: 'center',
+    zIndex: 20
   },
   container: {
     flexDirection: 'row',
-    backgroundColor: 'white',
+    backgroundColor: '#F8FAFC',
     borderRadius: 8,
-    padding: 8,
+    borderColor: '#D8E0E8',
+    borderWidth: 1,
+    padding: 10,
+    zIndex: 1,
     shadowColor: '#000',
     shadowOffset: {
       width: 0,
-      height: 2
+      height: 8
     },
-    shadowOpacity: 0.25,
-    shadowRadius: 4,
+    shadowOpacity: 0.14,
+    shadowRadius: 18,
     elevation: 5
   },
   pieceButton: {
+    alignItems: 'center',
+    justifyContent: 'center',
     padding: 8,
-    borderRadius: 4,
+    borderRadius: 7,
+    borderColor: '#CBD5E1',
+    borderWidth: 1,
     marginHorizontal: 4
   },
   pieceImage: {
@@ -43,31 +51,38 @@ export const PromotionDialog = /*#__PURE__*/React.memo(({
   const {
     colors
   } = config;
+  const animatePresentation = Platform.OS !== 'android';
   return /*#__PURE__*/React.createElement(Modal, {
     transparent: true,
     visible: true,
-    animationType: "fade",
+    animationType: animatePresentation ? "fade" : "none",
     onRequestClose: onCancel
-  }, /*#__PURE__*/React.createElement(Pressable, {
+  }, /*#__PURE__*/React.createElement(View, {
     style: styles.overlay,
+    testID: "promotion-dialog-overlay"
+  }, /*#__PURE__*/React.createElement(Pressable, {
+    style: StyleSheet.absoluteFillObject,
     onPress: onCancel
-  }, /*#__PURE__*/React.createElement(Animated.View, {
-    entering: FadeIn.duration(200),
-    exiting: FadeOut.duration(200),
-    style: styles.container
+  }), /*#__PURE__*/React.createElement(Animated.View, {
+    entering: animatePresentation ? FadeIn.duration(200) : undefined,
+    exiting: animatePresentation ? FadeOut.duration(200) : undefined,
+    style: styles.container,
+    testID: "promotion-dialog-container"
   }, PROMOTION_PIECES.map(piece => {
     const pieceCode = `${color}${piece}`;
-    const source = PIECE_SOURCES[pieceCode];
     return /*#__PURE__*/React.createElement(TouchableOpacity, {
       key: piece,
       style: [styles.pieceButton, {
         backgroundColor: colors.promotionPieceButton
       }],
+      testID: `promotion-choice-${piece}`,
       onPress: () => onSelect(piece),
-      activeOpacity: 0.7
+      activeOpacity: 0.7,
+      accessibilityLabel: `Promote to ${PROMOTION_PIECE_NAMES[piece]}`
     }, /*#__PURE__*/React.createElement(Image, {
-      source: source,
-      style: styles.pieceImage
+      source: PIECE_SOURCES[pieceCode],
+      style: styles.pieceImage,
+      testID: `promotion-choice-${piece}-image`
     }));
   }))));
 });
diff --git a/lib/module/components/skia/gesture-board.js b/lib/module/components/skia/gesture-board.js
index 79598f3cce08d079d2a004ed0563f1194fd7d31c..abd5a388b3146bf82650c6de75e346b1093ac8fb 100644
--- a/lib/module/components/skia/gesture-board.js
+++ b/lib/module/components/skia/gesture-board.js
@@ -1,4 +1,4 @@
-import React, { useMemo, useCallback, useState, forwardRef, useRef } from 'react';
+import React, { useMemo, useCallback, useState, useEffect, forwardRef, useRef } from 'react';
 import { View, StyleSheet } from 'react-native';
 import { GestureDetector, GestureHandlerRootView } from 'react-native-gesture-handler';
 import { useSharedValue, withSpring, withTiming } from 'react-native-reanimated';
@@ -13,13 +13,17 @@ import { SkiaBoard } from './skia-board';
 import { PromotionDialog } from '../promotion-dialog';
 const styles = StyleSheet.create({
   container: {
-    aspectRatio: 1
+    aspectRatio: 1,
+    position: 'relative'
   }
 });
 export const GestureBoard = /*#__PURE__*/forwardRef(({
   onMove,
   onIllegalMove,
   renderEffect,
+  onReady,
+  blocksExternalGesture,
+  positionKey,
   spriteSource
 }, ref) => {
   const {
@@ -30,6 +34,21 @@ export const GestureBoard = /*#__PURE__*/forwardRef(({
   const {
     image: spriteImage
   } = usePieceSpriteSheet(spriteSource);
+  useEffect(() => {
+    if (!spriteImage || !onReady) {
+      return;
+    }
+    let secondFrame = null;
+    const firstFrame = requestAnimationFrame(() => {
+      secondFrame = requestAnimationFrame(onReady);
+    });
+    return () => {
+      cancelAnimationFrame(firstFrame);
+      if (secondFrame !== null) {
+        cancelAnimationFrame(secondFrame);
+      }
+    };
+  }, [onReady, positionKey, spriteImage]);
 
   // Effect state for shader effects (all SharedValues for reactivity)
   const effectCenterX = useSharedValue(0);
@@ -79,6 +98,12 @@ export const GestureBoard = /*#__PURE__*/forwardRef(({
     promotionInfoRef.current = null;
     setShowPromotion(false);
   }, [boardState, config]);
+  const handleBoardReset = useCallback(() => {
+    const info = promotionInfoRef.current;
+    info === null || info === void 0 || info.cancel();
+    promotionInfoRef.current = null;
+    setShowPromotion(false);
+  }, []);
 
   // Trigger effect on game events
   const triggerEffect = useCallback((trigger, kingColor) => {
@@ -128,7 +153,8 @@ export const GestureBoard = /*#__PURE__*/forwardRef(({
     chess,
     boardState,
     moveExecutor,
-    defaultHighlightColor: config.colors.lastMoveHighlight
+    defaultHighlightColor: config.colors.lastMoveHighlight,
+    onBeforeReset: handleBoardReset
   });
   const gesture = useBoardGesture({
     boardState,
@@ -137,6 +163,7 @@ export const GestureBoard = /*#__PURE__*/forwardRef(({
     // Lock the board while the promotion picker is open so a second drag
     // can't start (and re-trigger) another promotion.
     gestureEnabled: config.gestureEnabled && !showPromotion,
+    blocksExternalGesture,
     onIllegalMove
   });
   const containerStyle = useMemo(() => [styles.container, {
diff --git a/lib/module/components/skia/skia-dots.js b/lib/module/components/skia/skia-dots.js
index 120f116f938d6bf19ed350321959c9c5a860b48a..863f4f5380a90afb2c2f1ed1508efe197b54782b 100644
--- a/lib/module/components/skia/skia-dots.js
+++ b/lib/module/components/skia/skia-dots.js
@@ -2,36 +2,58 @@ import React from 'react';
 import { Path, Skia } from '@shopify/react-native-skia';
 import { useDerivedValue } from 'react-native-reanimated';
 import { squareToPosition } from '../../state/use-board-state';
-// All valid-move dots are built by ONE derived path that reads the shared
-// `validMoves` once (a single subscription) and adds a circle per target —
-// instead of 64 per-square components each subscribing to `validMoves` and
-// scanning it. It re-runs ONLY when `validMoves` changes (not every frame),
-// so it costs nothing at rest. Dots are binary (no per-square opacity), so
-// one Path is a perfect fit.
+// Valid-move markers are built by two derived paths that read the shared
+// `validMoves` once. Empty targets use quiet dots; capture targets use a ring
+// so the marker remains visible when a piece already occupies the target.
 export const SkiaDots = /*#__PURE__*/React.memo(({
   config,
   boardState
 }) => {
   const {
+    colors,
     pieceSize,
     flipped
   } = config;
   const radius = pieceSize * 0.15;
+  const captureRadius = pieceSize * 0.38;
   const half = pieceSize / 2;
-  const path = useDerivedValue(() => {
-    const p = Skia.Path.Make();
+  const quietPath = useDerivedValue(() => {
+    const path = Skia.Path.Make();
     const moves = boardState.validMoves.get();
     for (let i = 0; i < moves.length; i++) {
-      const pos = squareToPosition(moves[i], pieceSize, flipped);
-      p.addCircle(pos.x + half, pos.y + half, radius);
+      const target = moves[i];
+      if (boardState.squares[target].piece.get()) {
+        continue;
+      }
+      const pos = squareToPosition(target, pieceSize, flipped);
+      path.addCircle(pos.x + half, pos.y + half, radius);
     }
-    return p;
+    return path;
   });
-  return /*#__PURE__*/React.createElement(Path, {
-    path: path,
-    color: "rgba(0, 0, 0, 0.3)",
-    opacity: 0.5
+  const capturePath = useDerivedValue(() => {
+    const path = Skia.Path.Make();
+    const moves = boardState.validMoves.get();
+    for (let i = 0; i < moves.length; i++) {
+      const target = moves[i];
+      if (!boardState.squares[target].piece.get()) {
+        continue;
+      }
+      const pos = squareToPosition(target, pieceSize, flipped);
+      path.addCircle(pos.x + half, pos.y + half, captureRadius);
+    }
+    return path;
   });
+  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Path, {
+    path: quietPath,
+    color: colors.validMoveDot,
+    opacity: 0.5
+  }), /*#__PURE__*/React.createElement(Path, {
+    path: capturePath,
+    color: colors.validMoveCapture,
+    opacity: 0.65,
+    style: "stroke",
+    strokeWidth: Math.max(2, pieceSize * 0.05)
+  }));
 });
 SkiaDots.displayName = 'SkiaDots';
 //# sourceMappingURL=skia-dots.js.map
\ No newline at end of file
diff --git a/lib/module/components/skia/skia-pieces-atlas.js b/lib/module/components/skia/skia-pieces-atlas.js
index efa0ca0d342d28c1d44eed586d831d294159c2d9..61a50214e449b4e31e358704aa703a3f9f744ae7 100644
--- a/lib/module/components/skia/skia-pieces-atlas.js
+++ b/lib/module/components/skia/skia-pieces-atlas.js
@@ -6,6 +6,7 @@ import { SQUARES } from '../../state/types';
 // Sprite sheet layout: 6x2 grid (p, n, b, r, q, k for each color)
 // Row 0: white pieces, Row 1: black pieces
 const SPRITE_CELL_SIZE = 128;
+const MAX_PIECE_SLOTS = 32;
 
 // Pre-computed sprite rects for all piece types
 const SPRITE_RECTS = {
@@ -76,6 +77,14 @@ export const SkiaPiecesAtlas = /*#__PURE__*/React.memo(({
       const scaledHalf = SPRITE_CELL_SIZE / 2 * pieceScale;
       transforms.push(Skia.RSXform(pieceScale, 0, centerX - scaledHalf, centerY - scaledHalf));
     }
+
+    // Atlas observes sprites and transforms through separate derived-value
+    // subscriptions. Keep both arrays at the board's fixed maximum piece
+    // count so an update cannot expose different lengths and crash Skia.
+    while (sprites.length < MAX_PIECE_SLOTS) {
+      sprites.push(SPRITE_RECTS.wp);
+      transforms.push(Skia.RSXform(0, 0, -pieceSize, -pieceSize));
+    }
     return {
       sprites,
       transforms
diff --git a/lib/module/hooks/use-board-gesture.js b/lib/module/hooks/use-board-gesture.js
index 504126e4dc0a469cf5b00913e176e360322a1194..c09174430003ba109c895bb474aad9eb484e6765 100644
--- a/lib/module/hooks/use-board-gesture.js
+++ b/lib/module/hooks/use-board-gesture.js
@@ -8,12 +8,14 @@ export const useBoardGesture = ({
   config,
   moveExecutor,
   gestureEnabled,
+  blocksExternalGesture,
   onIllegalMove
 }) => {
   const {
     pieceSize,
     animations,
-    flipped
+    flipped,
+    draggableColor
   } = config;
 
   // Track the currently dragged piece
@@ -23,11 +25,25 @@ export const useBoardGesture = ({
   // Offset between touch point and piece position (to follow finger exactly)
   const touchOffsetX = useSharedValue(0);
   const touchOffsetY = useSharedValue(0);
+  // Square that was already selected when the current touch began. pan.onBegin
+  // pre-selects the touched piece on touch-down, so the tap handler needs this
+  // to tell a "toggle off" tap from the tap that created the selection.
+  const selectedAtTouchStart = useSharedValue(null);
+  // True while a drag accepted by onStart is in flight. onEnd used to infer
+  // this from the square's zIndex, but a board reset (a fen-prop change
+  // landing mid-drag, e.g. right after an opponent-reply animation) zeroes
+  // zIndex, which made onEnd swallow the drop entirely — no tryMove, no
+  // onIllegalMove, and the sprite was left at the drop point.
+  const dragActivated = useSharedValue(false);
 
   // Stable callback for tryMove
-  const handleTryMove = useCallback((from, to) => {
-    moveExecutor.tryMove(from, to);
-  }, [moveExecutor]);
+  const handleTryMove = useCallback((from, to, notifyOnInvalid = false) => {
+    void moveExecutor.tryMove(from, to).then(move => {
+      if (!move && notifyOnInvalid) {
+        onIllegalMove === null || onIllegalMove === void 0 || onIllegalMove(from, to);
+      }
+    });
+  }, [moveExecutor, onIllegalMove]);
 
   // Stable callback for selectPiece
   const handleSelectPiece = useCallback(square => {
@@ -49,17 +65,32 @@ export const useBoardGesture = ({
         y
       } = event;
       const square = positionToSquare(x, y, pieceSize, flipped);
+      selectedAtTouchStart.set(boardState.selectedSquare.get());
       const squareState = boardState.squares[square];
       const piece = squareState.piece.get();
-      if (piece) {
-        // Store drag start info (but don't start drag yet)
-        draggedSquare.set(square);
-        dragStartX.set(squareState.translateX.get());
-        dragStartY.set(squareState.translateY.get());
-        const pieceCenterX = squareState.translateX.get() + pieceSize / 2;
-        const pieceCenterY = squareState.translateY.get() + pieceSize / 2;
-        touchOffsetX.set(x - pieceCenterX);
-        touchOffsetY.set(y - pieceCenterY);
+      const turn = boardState.turn.get();
+      const allowedDragColor = draggableColor ?? turn;
+      if (!piece || piece[0] !== allowedDragColor) {
+        draggedSquare.set(null);
+        return;
+      }
+
+      // Store drag start info (but don't start drag yet)
+      draggedSquare.set(square);
+      dragStartX.set(squareState.translateX.get());
+      dragStartY.set(squareState.translateY.get());
+      const pieceCenterX = squareState.translateX.get() + pieceSize / 2;
+      const pieceCenterY = squareState.translateY.get() + pieceSize / 2;
+      touchOffsetX.set(x - pieceCenterX);
+      touchOffsetY.set(y - pieceCenterY);
+
+      // Start the valid-target calculation before the drag crosses the
+      // min-distance threshold. Otherwise very fast drags can reach onEnd
+      // before the RN-thread select callback has populated validMoves.
+      // Skip when the square is already selected: validMoves are populated,
+      // and re-selecting could race a deselect issued by the tap handler.
+      if (boardState.selectedSquare.get() !== square) {
+        scheduleOnRN(handleSelectPiece, square);
       }
     }).onStart(() => {
       'worklet';
@@ -70,16 +101,23 @@ export const useBoardGesture = ({
       const squareState = boardState.squares[square];
       const piece = squareState.piece.get();
       const turn = boardState.turn.get();
+      const allowedDragColor = draggableColor ?? turn;
+      const isOwnPiece = piece && piece[0] === allowedDragColor;
+
+      // Do not start dragging opponent pieces; otherwise onUpdate can visibly
+      // move them until onEnd snaps them back.
+      if (!isOwnPiece) {
+        draggedSquare.set(null);
+        return;
+      }
 
       // Raise and scale the piece
+      dragActivated.set(true);
       squareState.zIndex.set(100);
       squareState.scale.set(withSpring(1.1, animations.scale));
 
       // Only show valid moves (dots) for own pieces
-      const isOwnPiece = piece && piece[0] === turn;
-      if (isOwnPiece) {
-        scheduleOnRN(handleSelectPiece, square);
-      }
+      scheduleOnRN(handleSelectPiece, square);
     }).onUpdate(event => {
       'worklet';
 
@@ -91,25 +129,45 @@ export const useBoardGesture = ({
       // Subtracting pieceSize/2 converts from center back to top-left (translateX/Y)
       squareState.translateX.set(event.x - touchOffsetX.get() - pieceSize / 2);
       squareState.translateY.set(event.y - touchOffsetY.get() - pieceSize / 2);
-    }).onEnd(() => {
+    }).onEnd((_event, success) => {
       'worklet';
 
       const square = draggedSquare.get();
       if (!square) return;
       const squareState = boardState.squares[square];
 
-      // Only process drop if piece was actually dragged (zIndex > 0)
-      const wasDragged = squareState.zIndex.get() > 0;
+      // Only process drop if the drag actually activated (onStart accepted
+      // it). Tracked in a gesture-local flag rather than the square's zIndex:
+      // board resets can land mid-drag, wipe zIndex, and must not swallow
+      // the drop.
+      const wasDragged = dragActivated.get();
+      dragActivated.set(false);
       if (!wasDragged) {
         draggedSquare.set(null);
         return;
       }
       squareState.scale.set(withSpring(1, animations.scale));
 
+      // The gesture was cancelled mid-drag (success === false: e.g. a board
+      // lock flipped `enabled` while the finger was down). Never derive a
+      // drop from wherever the cancellation caught the piece — snap it home
+      // and clear the selection like a failed drop would.
+      if (!success) {
+        const originalPos = squareToPosition(square, pieceSize, flipped);
+        squareState.translateX.set(withSpring(originalPos.x, animations.snapBack));
+        squareState.translateY.set(withSpring(originalPos.y, animations.snapBack));
+        squareState.zIndex.set(0);
+        boardState.selectedSquare.set(null);
+        boardState.validMoves.set([]);
+        draggedSquare.set(null);
+        return;
+      }
+
       // Check if this is own piece - only own pieces can make moves
       const piece = squareState.piece.get();
       const turn = boardState.turn.get();
-      const isOwnPiece = piece && piece[0] === turn;
+      const allowedDragColor = draggableColor ?? turn;
+      const isOwnPiece = piece && piece[0] === allowedDragColor;
 
       // If not own piece, just snap back (can't make moves with opponent's pieces)
       if (!isOwnPiece) {
@@ -141,6 +199,17 @@ export const useBoardGesture = ({
         draggedSquare.set(null);
         return;
       }
+      if (targetSquare !== square && validMoves.length === 0) {
+        const originalPos = squareToPosition(square, pieceSize, flipped);
+        squareState.translateX.set(originalPos.x);
+        squareState.translateY.set(originalPos.y);
+        squareState.zIndex.set(0);
+        boardState.selectedSquare.set(null);
+        boardState.validMoves.set([]);
+        scheduleOnRN(handleTryMove, square, targetSquare, true);
+        draggedSquare.set(null);
+        return;
+      }
 
       // Invalid move - snap back to original position and clear selection
       const originalPos = squareToPosition(square, pieceSize, flipped);
@@ -166,6 +235,7 @@ export const useBoardGesture = ({
           squareState.zIndex.set(0);
         }
       }
+      dragActivated.set(false);
       draggedSquare.set(null);
     });
 
@@ -181,20 +251,30 @@ export const useBoardGesture = ({
       const selectedSquare = boardState.selectedSquare.get();
       const piece = boardState.squares[square].piece.get();
       const turn = boardState.turn.get();
-      const isOwnPiece = piece && piece[0] === turn;
+      const allowedDragColor = draggableColor ?? turn;
+      const isOwnPiece = piece && piece[0] === allowedDragColor;
 
       // Case 1: No piece selected - try to select own piece
       if (!selectedSquare) {
         if (isOwnPiece) {
+          boardState.selectedSquare.set(square);
+          boardState.validMoves.set([]);
           scheduleOnRN(handleSelectPiece, square);
         }
         return;
       }
 
-      // Case 2: Tapped on same piece - deselect
+      // Case 2: Tapped on same piece - deselect, but only when the piece
+      // was already selected before this touch began. pan.onBegin selects
+      // the touched piece on touch-down, so without this guard every quick
+      // tap would read its own selection here and immediately toggle it
+      // off (dots flash and vanish; only >maxDuration presses survived
+      // because the failed tap never ran onEnd).
       if (square === selectedSquare) {
-        boardState.selectedSquare.set(null);
-        boardState.validMoves.set([]);
+        if (selectedAtTouchStart.get() === square) {
+          boardState.selectedSquare.set(null);
+          boardState.validMoves.set([]);
+        }
         return;
       }
 
@@ -207,18 +287,38 @@ export const useBoardGesture = ({
 
       // Case 4: Tapped on another own piece - switch selection
       if (isOwnPiece) {
+        boardState.selectedSquare.set(square);
+        boardState.validMoves.set([]);
         scheduleOnRN(handleSelectPiece, square);
         return;
       }
 
-      // Case 5: Invalid target - deselect
+      // Case 5: Valid moves may not have crossed from the RN thread to the
+      // UI worklet yet after a fast source tap. Let the executor make the
+      // final legality decision so tap-tap stays responsive.
+      if (validMoves.length === 0) {
+        boardState.selectedSquare.set(null);
+        boardState.validMoves.set([]);
+        scheduleOnRN(handleTryMove, selectedSquare, square, true);
+        return;
+      }
+
+      // Case 6: Invalid target - deselect
       boardState.selectedSquare.set(null);
       boardState.validMoves.set([]);
     });
 
-    // Combine gestures - pan takes precedence
-    return Gesture.Simultaneous(tapGesture, panGesture);
-  }, [boardState, pieceSize, flipped, draggedSquare, dragStartX, dragStartY, touchOffsetX, touchOffsetY, animations, gestureEnabled, handleTryMove, handleSelectPiece, handleIllegalMove]);
+    // Relations attached to a composed Race are not propagated to its raw
+    // gestures. Block the parent scroll from both candidates before composing.
+    if (blocksExternalGesture) {
+      tapGesture.blocksExternalGesture(blocksExternalGesture);
+      panGesture.blocksExternalGesture(blocksExternalGesture);
+    }
+
+    // Tap should not wait for the pan candidate to settle. A real drag still
+    // wins once it crosses minDistance before the tap can activate on release.
+    return Gesture.Race(tapGesture, panGesture);
+  }, [boardState, pieceSize, flipped, draggedSquare, dragStartX, dragStartY, touchOffsetX, touchOffsetY, selectedAtTouchStart, dragActivated, animations, gestureEnabled, blocksExternalGesture, draggableColor, handleTryMove, handleSelectPiece, handleIllegalMove]);
   return gesture;
 };
 //# sourceMappingURL=use-board-gesture.js.map
\ No newline at end of file
diff --git a/lib/module/hooks/use-chessboard-ref.js b/lib/module/hooks/use-chessboard-ref.js
index 0c7aa00855119b0b99285596e024cf51f18409ce..ffb0af8cf082f8d5383f579fa62b5482b8e4e783 100644
--- a/lib/module/hooks/use-chessboard-ref.js
+++ b/lib/module/hooks/use-chessboard-ref.js
@@ -6,7 +6,8 @@ export const useChessboardRef = ({
   chess,
   boardState,
   moveExecutor,
-  defaultHighlightColor = 'rgba(255, 255, 0, 0.5)'
+  defaultHighlightColor = 'rgba(255, 255, 0, 0.5)',
+  onBeforeReset
 }) => {
   const move = useCallback(async params => {
     return moveExecutor.tryMove(params.from, params.to, params.promotion);
@@ -30,8 +31,9 @@ export const useChessboardRef = ({
     boardState.kingInCheckSquare.set(null);
   }, [boardState.highlights, boardState.squares, boardState.lastMove, boardState.kingInCheckSquare]);
   const resetBoard = useCallback((fen, opts) => {
-    moveExecutor.resetBoard(fen, opts);
-  }, [moveExecutor]);
+    onBeforeReset === null || onBeforeReset === void 0 || onBeforeReset();
+    return moveExecutor.resetBoard(fen, opts);
+  }, [moveExecutor, onBeforeReset]);
   const getState = useCallback(() => {
     return getChessboardState(chess);
   }, [chess]);
diff --git a/lib/module/index.js b/lib/module/index.js
index 7cee33e75529b83bea5c7216e4b89be54c9fb520..ad70cac42f9a03c9934ec526a71a19339680a629 100644
--- a/lib/module/index.js
+++ b/lib/module/index.js
@@ -5,6 +5,7 @@ const Chessboard = /*#__PURE__*/forwardRef(({
   fen,
   boardSize,
   gestureEnabled,
+  draggableColor,
   flipped,
   withLetters,
   withNumbers,
@@ -13,6 +14,8 @@ const Chessboard = /*#__PURE__*/forwardRef(({
   onMove,
   onIllegalMove,
   renderEffect,
+  onReady,
+  blocksExternalGesture,
   spriteSource,
   fontSource
 }, ref) => {
@@ -20,6 +23,7 @@ const Chessboard = /*#__PURE__*/forwardRef(({
     fen: fen,
     boardSize: boardSize,
     gestureEnabled: gestureEnabled,
+    draggableColor: draggableColor,
     flipped: flipped,
     withLetters: withLetters,
     withNumbers: withNumbers,
@@ -31,6 +35,9 @@ const Chessboard = /*#__PURE__*/forwardRef(({
     onMove: onMove,
     onIllegalMove: onIllegalMove,
     renderEffect: renderEffect,
+    onReady: onReady,
+    blocksExternalGesture: blocksExternalGesture,
+    positionKey: fen,
     spriteSource: spriteSource
   }));
 });
diff --git a/lib/module/state/board-state-context.js b/lib/module/state/board-state-context.js
index 9c5fc37c72a09fa6a338aba03bd0433d0c09573e..9c781af9859d91627effe7113c653689ad84ac7e 100644
--- a/lib/module/state/board-state-context.js
+++ b/lib/module/state/board-state-context.js
@@ -12,7 +12,9 @@ const defaultColors = {
   black: '#62B1A8',
   lastMoveHighlight: 'rgba(255,255,0, 0.5)',
   checkmateHighlight: '#E84855',
-  promotionPieceButton: '#FF9B71'
+  promotionPieceButton: '#FF9B71',
+  validMoveDot: 'rgba(0, 0, 0, 0.3)',
+  validMoveCapture: 'rgba(0, 0, 0, 0.55)'
 };
 const defaultDurations = {
   move: 150
@@ -27,6 +29,7 @@ export const BoardStateProvider = ({
   fen,
   boardSize = DEFAULT_BOARD_SIZE,
   gestureEnabled = true,
+  draggableColor = null,
   flipped = false,
   withLetters = true,
   withNumbers = true,
@@ -39,6 +42,7 @@ export const BoardStateProvider = ({
     boardSize,
     pieceSize,
     gestureEnabled,
+    draggableColor,
     flipped,
     withLetters,
     withNumbers,
@@ -52,7 +56,7 @@ export const BoardStateProvider = ({
     },
     animations: defaultAnimations,
     fontSource: fontSource ?? null
-  }), [boardSize, pieceSize, gestureEnabled, flipped, withLetters, withNumbers, colors, durations, fontSource]);
+  }), [boardSize, pieceSize, gestureEnabled, draggableColor, flipped, withLetters, withNumbers, colors, durations, fontSource]);
   const {
     boardState,
     chess
diff --git a/lib/module/state/move-executor.js b/lib/module/state/move-executor.js
index f2f179f71c94846c9cbe456e5889ac1b5c0714d9..d3768a64a1c06447481b8febd223bec9d38ae9f8 100644
--- a/lib/module/state/move-executor.js
+++ b/lib/module/state/move-executor.js
@@ -1,5 +1,5 @@
-import { withSpring } from 'react-native-reanimated';
-import { scheduleOnRN } from 'react-native-worklets';
+import { withSpring, withTiming } from 'react-native-reanimated';
+import { scheduleOnRN, scheduleOnUI } from 'react-native-worklets';
 import { squareToPosition } from './use-board-state';
 import { getChessboardState } from '../helpers/get-chessboard-state';
 import { findKingSquare } from '../helpers/find-king-square';
@@ -55,51 +55,56 @@ export const createMoveExecutor = (chess, boardState, config, callbacks) => {
     if (!move) return null;
     const fromState = boardState.squares[from];
     const toState = boardState.squares[to];
-    const movingPiece = fromState.piece.get();
-
-    // Handle capture - clear target square piece
-    if (move.captured) {
-      toState.piece.set(null);
-    }
 
     // Animate the piece
     const toPos = squareToPosition(to, pieceSize, flipped);
     const fromPos = squareToPosition(from, pieceSize, flipped);
 
-    // Raise the moving piece
-    fromState.zIndex.set(100);
+    // Pre-compute the final piece code to avoid capturing complex objects in
+    // worklet. Derived from the chess move (not the origin sprite slot) so a
+    // sprite-level desync can never plant a null piece on the destination.
+    const finalPieceCode = promotionPiece ? `${move.color}${promotionPiece}` : `${move.color}${move.piece}`;
 
-    // Pre-compute the final piece code to avoid capturing complex objects in worklet
-    const finalPieceCode = promotionPiece ? `${move.color}${promotionPiece}` : movingPiece;
+    // Migrate the piece to its destination slot immediately, so the sprite
+    // slots always match the chess position (chess.move already ran above).
+    // Parking the piece in the origin slot until the glide settled — the old
+    // flow — made a fast re-grab of a just-moved piece find an empty
+    // destination square, and any interruption of the glide (board reset,
+    // premove replay) left the sprite stranded on the wrong slot. The glide
+    // renders from the destination slot instead, starting wherever the piece
+    // currently is: the origin square for tap moves, or the drop point for
+    // drags. Setting the destination piece also consumes any captured piece.
+    const startX = fromState.translateX.get();
+    const startY = fromState.translateY.get();
+    fromState.piece.set(null);
+    fromState.translateX.set(fromPos.x);
+    fromState.translateY.set(fromPos.y);
+    fromState.zIndex.set(0);
+    toState.piece.set(finalPieceCode);
+    toState.translateX.set(startX);
+    toState.translateY.set(startY);
+    toState.zIndex.set(100);
     const commitMove = finished => {
       'worklet';
 
-      // Only commit the sprite writes if the spring actually settled. A
-      // cancelled spring (resetBoard, or a newer animation on the same
-      // square) means whoever cancelled it now owns this square's state —
-      // writing here would smear stale pieces onto the fresh board.
+      // The slots migrated at move time; settling only lowers the piece. A
+      // cancelled glide (resetBoard, a re-grab, or a newer animation on the
+      // same square) means whoever cancelled it owns the square's state now.
       if (finished) {
-        // Move complete - update piece positions
-        toState.piece.set(finalPieceCode);
-        fromState.piece.set(null);
-
-        // Reset position to original square for future use
-        fromState.translateX.set(fromPos.x);
-        fromState.translateY.set(fromPos.y);
-        fromState.zIndex.set(0);
+        toState.zIndex.set(0);
       }
       if (onAnimationComplete) {
         scheduleOnRN(onAnimationComplete);
       }
     };
 
-    // Attach the commit to an axis that actually travels. A spring whose
-    // target equals its current value settles immediately — putting the
-    // callback there commits the move before any motion happens, so the
-    // piece teleports (horizontal moves have dy === 0).
-    const movesVertically = toPos.y !== fromPos.y;
-    fromState.translateX.set(withSpring(toPos.x, animations.move, movesVertically ? undefined : commitMove));
-    fromState.translateY.set(withSpring(toPos.y, animations.move, movesVertically ? commitMove : undefined));
+    // Attach the commit to the axis with the larger travel. A spring whose
+    // target equals its current value settles immediately — riding a
+    // no-travel axis would fire the commit (and resolve tryMove) before any
+    // motion happens.
+    const movesVertically = Math.abs(toPos.y - startY) >= Math.abs(toPos.x - startX);
+    toState.translateX.set(withSpring(toPos.x, animations.move, movesVertically ? undefined : commitMove));
+    toState.translateY.set(withSpring(toPos.y, animations.move, movesVertically ? commitMove : undefined));
 
     // Handle castling - move the rook
     if (move.flags.includes('k') || move.flags.includes('q')) {
@@ -109,22 +114,22 @@ export const createMoveExecutor = (chess, boardState, config, callbacks) => {
       const rookTo = (isKingside ? 'f' : 'd') + rank;
       const rookFromState = boardState.squares[rookFrom];
       const rookToState = boardState.squares[rookTo];
-      const rookPiece = rookFromState.piece.get();
+      const rookPiece = `${move.color}r`;
       const rookToPos = squareToPosition(rookTo, pieceSize, flipped);
       const rookFromPos = squareToPosition(rookFrom, pieceSize, flipped);
 
-      // The rook slides along its rank — horizontal — so the commit must
-      // ride the X spring (the Y spring settles instantly; see commitMove).
-      rookFromState.translateX.set(withSpring(rookToPos.x, animations.move, finished => {
-        'worklet';
-
-        if (!finished) return;
-        rookToState.piece.set(rookPiece);
-        rookFromState.piece.set(null);
-        rookFromState.translateX.set(rookFromPos.x);
-        rookFromState.translateY.set(rookFromPos.y);
-      }));
-      rookFromState.translateY.set(withSpring(rookToPos.y, animations.move));
+      // Same immediate slot migration as the moving piece: the rook lives in
+      // its destination slot from move time and glides in from its origin.
+      const rookStartX = rookFromState.translateX.get();
+      const rookStartY = rookFromState.translateY.get();
+      rookFromState.piece.set(null);
+      rookFromState.translateX.set(rookFromPos.x);
+      rookFromState.translateY.set(rookFromPos.y);
+      rookToState.piece.set(rookPiece);
+      rookToState.translateX.set(rookStartX);
+      rookToState.translateY.set(rookStartY);
+      rookToState.translateX.set(withSpring(rookToPos.x, animations.move));
+      rookToState.translateY.set(withSpring(rookToPos.y, animations.move));
     }
 
     // Handle en passant - remove the captured pawn
@@ -234,82 +239,85 @@ export const createMoveExecutor = (chess, boardState, config, callbacks) => {
       } catch {
         // Invalid FEN — leave the chess instance untouched and bail out so
         // the board state stays consistent with what's actually on screen.
-        return;
+        return Promise.resolve();
       }
     } else {
       chess.reset();
     }
     const slide = opts === null || opts === void 0 ? void 0 : opts.slide;
     const lastMove = (opts === null || opts === void 0 ? void 0 : opts.lastMove) ?? null;
+    const targetFlipped = (opts === null || opts === void 0 ? void 0 : opts.flipped) ?? flipped;
     const board = chess.board();
-
-    // Update every square. When `slide` is given, the piece landing on
-    // `slide.to` starts at `slide.from` and springs home — so stepping through
-    // a game's history animates the moved piece instead of snapping.
+    const squareSnapshots = [];
     for (let row = 0; row < 8; row++) {
       for (let col = 0; col < 8; col++) {
         const colChar = String.fromCharCode('a'.charCodeAt(0) + col);
         const rowNum = 8 - row;
         const square = `${colChar}${rowNum}`;
-        const sq = boardState.squares[square];
         const piece = board[row][col];
-        sq.piece.set(piece ? `${piece.color}${piece.type}` : null);
-        sq.scale.set(1);
-        sq.lastMove.set(!!lastMove && (square === lastMove.from || square === lastMove.to));
-        sq.inCheck.set(false);
-        const pos = squareToPosition(square, pieceSize, flipped);
-        if (slide && square === slide.to) {
-          const fromPos = squareToPosition(slide.from, pieceSize, flipped);
-          sq.zIndex.set(100);
-          sq.translateX.set(fromPos.x);
-          sq.translateY.set(fromPos.y);
-          // zIndex drop rides the axis that actually travels (a no-travel
-          // spring settles instantly — see commitMove in executeMove).
-          const slidesVertically = pos.y !== fromPos.y;
-          const dropZIndex = () => {
-            'worklet';
-
-            sq.zIndex.set(0);
-          };
-          sq.translateX.set(withSpring(pos.x, animations.move, slidesVertically ? undefined : dropZIndex));
-          sq.translateY.set(withSpring(pos.y, animations.move, slidesVertically ? dropZIndex : undefined));
-        } else {
-          sq.translateX.set(pos.x);
-          sq.translateY.set(pos.y);
-          sq.zIndex.set(0);
-        }
+        const pos = squareToPosition(square, pieceSize, targetFlipped);
+        squareSnapshots.push({
+          square,
+          piece: piece ? `${piece.color}${piece.type}` : null,
+          x: pos.x,
+          y: pos.y
+        });
       }
     }
-
-    // Reset other state
-    boardState.turn.set(chess.turn());
-    boardState.selectedSquare.set(null);
-    boardState.validMoves.set([]);
-    boardState.lastMove.set(lastMove);
-
-    // Check / checkmate highlight for the resulting position.
+    const turn = chess.turn();
     const isInCheck = chess.isCheck();
-    const kingSquare = isInCheck || chess.isCheckmate() ? findKingSquare(chess, chess.turn()) : null;
-    if (kingSquare) {
-      boardState.squares[kingSquare].inCheck.set(true);
-    }
-    boardState.isCheck.set(isInCheck);
-    boardState.kingInCheckSquare.set(kingSquare);
+    const kingSquare = isInCheck || chess.isCheckmate() ? findKingSquare(chess, turn) : null;
+    const slideFromPosition = slide ? squareToPosition(slide.from, pieceSize, targetFlipped) : null;
+    return new Promise(resolve => {
+      scheduleOnUI((state, snapshots, nextTurn, nextLastMove, nextKingSquare, nextIsInCheck, nextSlide, nextSlideFromPosition, moveAnimation, effects, onCommitted) => {
+        'worklet';
 
-    // Clear highlights
-    for (const square of Object.keys(boardState.highlights)) {
-      boardState.highlights[square].color.set(null);
-    }
+        for (const snapshot of snapshots) {
+          const sq = state.squares[snapshot.square];
+          sq.piece.set(snapshot.piece);
+          sq.scale.set(1);
+          sq.lastMove.set(!!nextLastMove && (snapshot.square === nextLastMove.from || snapshot.square === nextLastMove.to));
+          sq.inCheck.set(snapshot.square === nextKingSquare);
+          if (nextSlide && nextSlideFromPosition && snapshot.square === nextSlide.to) {
+            const fromPos = nextSlideFromPosition;
+            sq.zIndex.set(100);
+            sq.translateX.set(fromPos.x);
+            sq.translateY.set(fromPos.y);
+            const slidesVertically = snapshot.y !== fromPos.y;
+            const dropZIndex = () => {
+              'worklet';
 
-    // Reset shader effect SharedValues so a fresh game's first check or
-    // checkmate doesn't trigger a ripple at the previous game's king
-    // square (centerX/centerY were last written by triggerEffect).
-    if (callbacks.effectSharedValues) {
-      callbacks.effectSharedValues.centerX.set(0);
-      callbacks.effectSharedValues.centerY.set(0);
-      callbacks.effectSharedValues.progress.set(0);
-      callbacks.effectSharedValues.trigger.set('');
-    }
+              sq.zIndex.set(0);
+            };
+            const timing = nextSlide.durationMs === undefined ? null : {
+              duration: nextSlide.durationMs
+            };
+            sq.translateX.set(timing ? withTiming(snapshot.x, timing, slidesVertically ? undefined : dropZIndex) : withSpring(snapshot.x, moveAnimation, slidesVertically ? undefined : dropZIndex));
+            sq.translateY.set(timing ? withTiming(snapshot.y, timing, slidesVertically ? dropZIndex : undefined) : withSpring(snapshot.y, moveAnimation, slidesVertically ? dropZIndex : undefined));
+          } else {
+            sq.translateX.set(snapshot.x);
+            sq.translateY.set(snapshot.y);
+            sq.zIndex.set(0);
+          }
+        }
+        state.turn.set(nextTurn);
+        state.selectedSquare.set(null);
+        state.validMoves.set([]);
+        state.lastMove.set(nextLastMove);
+        state.isCheck.set(nextIsInCheck);
+        state.kingInCheckSquare.set(nextKingSquare);
+        for (const square of Object.keys(state.highlights)) {
+          state.highlights[square].color.set(null);
+        }
+        if (effects) {
+          effects.centerX.set(0);
+          effects.centerY.set(0);
+          effects.progress.set(0);
+          effects.trigger.set('');
+        }
+        scheduleOnRN(onCommitted);
+      }, boardState, squareSnapshots, turn, lastMove, kingSquare, isInCheck, slide, slideFromPosition, animations.move, callbacks.effectSharedValues, resolve);
+    });
   };
   const undo = () => {
     const move = chess.undo();
diff --git a/lib/module/state/use-board-state.js b/lib/module/state/use-board-state.js
index 70c2cecb9088e81c71bbda522458e5602b3ccfe6..5445a48d5e9faf642f4462debe9b09893f0a0c5f 100644
--- a/lib/module/state/use-board-state.js
+++ b/lib/module/state/use-board-state.js
@@ -117,8 +117,11 @@ export const useBoardState = (initialFen, pieceSize, flipped = false) => {
   // squareStates were created once with the *initial* flipped value, so toggling
   // `flipped` at runtime would otherwise leave un-moved pieces stuck at their
   // original positions. A flip/resize is an orientation/scale change, not a
-  // move, so snap every occupied square to its new logical position instantly
-  // — springing them would make all pieces fly across the board at once.
+  // move, so snap every logical square slot to its new position instantly —
+  // springing them would make all pieces fly across the board at once. Do not
+  // consult the piece SharedValue here: resetBoard writes can still be queued
+  // on Android when this effect runs, so its current value may describe the
+  // previous FEN and cause newly occupied squares to keep the old orientation.
   const isFirstLayoutRef = useRef(true);
   useEffect(() => {
     if (isFirstLayoutRef.current) {
@@ -127,7 +130,6 @@ export const useBoardState = (initialFen, pieceSize, flipped = false) => {
     }
     for (const square of SQUARES) {
       const state = squareStates[square];
-      if (state.piece.get() === null) continue;
       const pos = squareToPosition(square, pieceSize, flipped);
       state.translateX.set(pos.x);
       state.translateY.set(pos.y);
@@ -147,6 +149,14 @@ export const useBoardState = (initialFen, pieceSize, flipped = false) => {
       isFirstFenRef.current = false;
       return;
     }
+    // The screen aligns the fen prop with a position the board already
+    // reached through animated moves (opponent replies, premove replays).
+    // When the position already matches, the rewrite below would be a no-op
+    // for the pieces but would still wipe live gesture state — selection,
+    // valid-move dots, and an in-flight drag (zIndex) — mid-gesture. Skip it.
+    if (initialFen && chess.fen() === initialFen) {
+      return;
+    }
     if (!initialFen) {
       chess.reset();
     } else {
diff --git a/lib/typescript/components/skia/gesture-board.d.ts b/lib/typescript/components/skia/gesture-board.d.ts
index 879929871b8cf9abe7953a6992f7c038f0592d5f..0cf3fb49da52ead8bed993476d48d67e0b0884ee 100644
--- a/lib/typescript/components/skia/gesture-board.d.ts
+++ b/lib/typescript/components/skia/gesture-board.d.ts
@@ -1,5 +1,6 @@
 import React from 'react';
 import type { ImageSourcePropType } from 'react-native';
+import type { GestureType } from 'react-native-gesture-handler';
 import type { Square } from 'chess.js';
 import { MoveResult } from '../../state/move-executor';
 import { ChessboardRef } from '../../hooks/use-chessboard-ref';
@@ -8,6 +9,9 @@ export interface GestureBoardProps {
     onMove?: (result: MoveResult) => void;
     onIllegalMove?: (from: Square, to: Square) => void;
     renderEffect?: (params: EffectParams) => React.ReactNode;
+    onReady?: () => void;
+    blocksExternalGesture?: React.RefObject<GestureType | undefined>;
+    positionKey?: string;
     spriteSource?: ImageSourcePropType;
 }
 export declare const GestureBoard: React.ForwardRefExoticComponent<GestureBoardProps & React.RefAttributes<ChessboardRef>>;
diff --git a/lib/typescript/hooks/use-board-gesture.d.ts b/lib/typescript/hooks/use-board-gesture.d.ts
index fd3bbda6ebc9d343270cc92a0e1c37023849b8b8..89f80d19e89890f8619dfdbfc0a813950466b7ed 100644
--- a/lib/typescript/hooks/use-board-gesture.d.ts
+++ b/lib/typescript/hooks/use-board-gesture.d.ts
@@ -1,12 +1,15 @@
 import type { Square } from 'chess.js';
+import type { RefObject } from 'react';
 import type { BoardState, BoardConfig } from '../state/types';
 import type { MoveExecutor } from '../state/move-executor';
+import type { GestureType } from 'react-native-gesture-handler';
 interface UseBoardGestureProps {
     boardState: BoardState;
     config: BoardConfig;
     moveExecutor: MoveExecutor;
     gestureEnabled: boolean;
+    blocksExternalGesture?: RefObject<GestureType | undefined>;
     onIllegalMove?: (from: Square, to: Square) => void;
 }
-export declare const useBoardGesture: ({ boardState, config, moveExecutor, gestureEnabled, onIllegalMove, }: UseBoardGestureProps) => import("react-native-gesture-handler/lib/typescript/handlers/gestures/gestureComposition").SimultaneousGesture;
+export declare const useBoardGesture: ({ boardState, config, moveExecutor, gestureEnabled, blocksExternalGesture, onIllegalMove, }: UseBoardGestureProps) => import("react-native-gesture-handler/lib/typescript/handlers/gestures/gestureComposition").SimultaneousGesture;
 export {};
diff --git a/lib/typescript/hooks/use-chessboard-ref.d.ts b/lib/typescript/hooks/use-chessboard-ref.d.ts
index b3a8a75fde04559b7e81ef8b8aef53d9081e5924..2891ce73615664cb9c791ee6d3e813ced02e89eb 100644
--- a/lib/typescript/hooks/use-chessboard-ref.d.ts
+++ b/lib/typescript/hooks/use-chessboard-ref.d.ts
@@ -20,12 +20,14 @@ export interface ChessboardRef {
         slide?: {
             from: Square;
             to: Square;
+            durationMs?: number;
         };
         lastMove?: {
             from: Square;
             to: Square;
         } | null;
-    }) => void;
+        flipped?: boolean;
+    }) => Promise<void>;
     getState: () => ChessboardState;
 }
 interface UseChessboardRefProps {
diff --git a/lib/typescript/index.d.ts b/lib/typescript/index.d.ts
index ac1a1357c8b5352dd97d856282ef96f056f45622..dff5c745aaa140b59141673e76365c033f9dac95 100644
--- a/lib/typescript/index.d.ts
+++ b/lib/typescript/index.d.ts
@@ -1,5 +1,6 @@
 import React from 'react';
 import type { ImageSourcePropType } from 'react-native';
+import type { GestureType } from 'react-native-gesture-handler';
 import type { Move, Square } from 'chess.js';
 import type { BoardStateProviderProps } from './state';
 import type { MoveResult } from './state/move-executor';
@@ -10,6 +11,13 @@ export interface ChessboardProps extends Omit<BoardStateProviderProps, 'children
     onMove?: (result: MoveResult) => void;
     onIllegalMove?: (from: Square, to: Square) => void;
     renderEffect?: (params: EffectParams) => React.ReactNode;
+    /**
+     * Called after the current position and piece sprite have had a native frame
+     * to paint. Called again when the `fen` prop changes.
+     */
+    onReady?: () => void;
+    /** Native gesture that the board's tap and pan gestures must block. */
+    blocksExternalGesture?: React.RefObject<GestureType | undefined>;
     /**
      * Optional custom piece sprite sheet. Must match the standard
      * 6×2 / 128px-cell layout (row 0 = white p,n,b,r,q,k; row 1 =
diff --git a/lib/typescript/state/board-state-context.d.ts b/lib/typescript/state/board-state-context.d.ts
index 9df5d29a4e894d52f6ff4c7036342a60eb2529f7..a1179eda7258a44362521fceb030b5a2f3595fef 100644
--- a/lib/typescript/state/board-state-context.d.ts
+++ b/lib/typescript/state/board-state-context.d.ts
@@ -1,5 +1,6 @@
 import React from 'react';
 import { Chess } from 'chess.js';
+import type { Color } from 'chess.js';
 import type { BoardState, BoardConfig } from './types';
 import type { ImageSourcePropType } from 'react-native';
 type BoardContextValue = {
@@ -12,6 +13,7 @@ export type BoardStateProviderProps = {
     fen?: string;
     boardSize?: number;
     gestureEnabled?: boolean;
+    draggableColor?: Color | null;
     flipped?: boolean;
     withLetters?: boolean;
     withNumbers?: boolean;
diff --git a/lib/typescript/state/move-executor.d.ts b/lib/typescript/state/move-executor.d.ts
index 6bef8348a4fd8cde7955491eaca48e8da844d89b..7651fa22d9200ee42f6c719c5a7bcb9b4122951e 100644
--- a/lib/typescript/state/move-executor.d.ts
+++ b/lib/typescript/state/move-executor.d.ts
@@ -36,12 +36,14 @@ export declare const createMoveExecutor: (chess: Chess, boardState: BoardState,
         slide?: {
             from: Square;
             to: Square;
+            durationMs?: number;
         };
         lastMove?: {
             from: Square;
             to: Square;
         } | null;
-    }) => void;
+        flipped?: boolean;
+    }) => Promise<void>;
     undo: () => Move | null;
 };
 export type MoveExecutor = ReturnType<typeof createMoveExecutor>;
diff --git a/lib/typescript/state/types.d.ts b/lib/typescript/state/types.d.ts
index eaac9e98118247d6e6776fe990fa7e10aa5b9227..2f896b9f44846bf23b0cbac42e69da0176304d9a 100644
--- a/lib/typescript/state/types.d.ts
+++ b/lib/typescript/state/types.d.ts
@@ -31,6 +31,7 @@ export interface BoardConfig {
     boardSize: number;
     pieceSize: number;
     gestureEnabled: boolean;
+    draggableColor: Color | null;
     flipped: boolean;
     withLetters: boolean;
     withNumbers: boolean;
@@ -40,6 +41,8 @@ export interface BoardConfig {
         lastMoveHighlight: string;
         checkmateHighlight: string;
         promotionPieceButton: string;
+        validMoveDot: string;
+        validMoveCapture: string;
     };
     durations: {
         move: number;
diff --git a/src/components/promotion-dialog.tsx b/src/components/promotion-dialog.tsx
index a3f67bf02f45b674772813bdf03f4b25c56f070e..133a12df3289ac45a55b3cce5c717b778f0455e0 100644
--- a/src/components/promotion-dialog.tsx
+++ b/src/components/promotion-dialog.tsx
@@ -4,7 +4,9 @@ import {
   TouchableOpacity,
   Image,
   Modal,
+  Platform,
   Pressable,
+  View,
 } from 'react-native';
 import type { PieceSymbol } from 'chess.js';
 import Animated, { FadeIn, FadeOut } from 'react-native-reanimated';
@@ -16 +18,2 @@ import { PIECE_SOURCES } from '../assets/piece-images';
-const PROMOTION_PIECES: PieceSymbol[] = ['q', 'r', 'b', 'n'];
+const PROMOTION_PIECES = ['q', 'r', 'b', 'n'] as const satisfies readonly PieceSymbol[];
+const PROMOTION_PIECE_NAMES: Record<(typeof PROMOTION_PIECES)[number], string> = { q: 'queen', r: 'rook', b: 'bishop', n: 'knight' };
@@ -20,27 +23,45 @@ interface PromotionDialogProps {
   config: BoardConfig;
 }
 
+const debugChessboard = (event: string, details: Record<string, unknown>) => {
+  const globals = globalThis as unknown as {
+    __CHESSTICIZE_CHESSBOARD_DEBUG__?: boolean;
+    __CHESSTICIZE_CHESSBOARD_DEBUG_SINK__?: (event: string, details: Record<string, unknown>) => void;
+  };
+  if (!globals.__CHESSTICIZE_CHESSBOARD_DEBUG__) return;
+  globals.__CHESSTICIZE_CHESSBOARD_DEBUG_SINK__?.(event, details);
+  console.warn(`[ChessboardDebug] ${event} ${JSON.stringify(details)}`);
+};
+
 const styles = StyleSheet.create({
   overlay: {
-    ...StyleSheet.absoluteFillObject,
-    backgroundColor: 'rgba(0, 0, 0, 0.5)',
+    flex: 1,
+    backgroundColor: 'rgba(15, 23, 42, 0.38)',
     justifyContent: 'center',
     alignItems: 'center',
+    zIndex: 20,
   },
   container: {
     flexDirection: 'row',
-    backgroundColor: 'white',
+    backgroundColor: '#F8FAFC',
     borderRadius: 8,
-    padding: 8,
+    borderColor: '#D8E0E8',
+    borderWidth: 1,
+    padding: 10,
+    zIndex: 1,
     shadowColor: '#000',
-    shadowOffset: { width: 0, height: 2 },
-    shadowOpacity: 0.25,
-    shadowRadius: 4,
+    shadowOffset: { width: 0, height: 8 },
+    shadowOpacity: 0.14,
+    shadowRadius: 18,
     elevation: 5,
   },
   pieceButton: {
+    alignItems: 'center',
+    justifyContent: 'center',
     padding: 8,
-    borderRadius: 4,
+    borderRadius: 7,
+    borderColor: '#CBD5E1',
+    borderWidth: 1,
     marginHorizontal: 4,
   },
   pieceImage: {
@@ -52,19 +73,26 @@ const styles = StyleSheet.create({
 export const PromotionDialog: React.FC<PromotionDialogProps> = React.memo(
   ({ color, onSelect, onCancel, config }) => {
     const { colors } = config;
+    const animatePresentation = Platform.OS !== 'android';
 
     return (
-      <Modal transparent visible animationType="fade" onRequestClose={onCancel}>
-        <Pressable style={styles.overlay} onPress={onCancel}>
+      <Modal
+        transparent
+        visible
+        animationType={animatePresentation ? 'fade' : 'none'}
+        onRequestClose={onCancel}
+      >
+        <View style={styles.overlay} testID="promotion-dialog-overlay">
+          <Pressable style={StyleSheet.absoluteFillObject} onPress={onCancel} />
           <Animated.View
-            entering={FadeIn.duration(200)}
-            exiting={FadeOut.duration(200)}
+            entering={animatePresentation ? FadeIn.duration(200) : undefined}
+            exiting={animatePresentation ? FadeOut.duration(200) : undefined}
             style={styles.container}
+            testID="promotion-dialog-container"
           >
             {PROMOTION_PIECES.map((piece) => {
               const pieceCode =
                 `${color}${piece}` as keyof typeof PIECE_SOURCES;
-              const source = PIECE_SOURCES[pieceCode];
 
               return (
                 <TouchableOpacity
@@ -73,15 +101,24 @@ export const PromotionDialog: React.FC<PromotionDialogProps> = React.memo(
                     styles.pieceButton,
                     { backgroundColor: colors.promotionPieceButton },
                   ]}
-                  onPress={() => onSelect(piece)}
+                  testID={`promotion-choice-${piece}`}
+                  onPress={() => {
+                    debugChessboard('promotionDialog:press', { piece });
+                    onSelect(piece);
+                  }}
                   activeOpacity={0.7}
+                  accessibilityLabel={`Promote to ${PROMOTION_PIECE_NAMES[piece]}`}
                 >
-                  <Image source={source} style={styles.pieceImage} />
+                  <Image
+                    source={PIECE_SOURCES[pieceCode]}
+                    style={styles.pieceImage}
+                    testID={`promotion-choice-${piece}-image`}
+                  />
                 </TouchableOpacity>
               );
             })}
           </Animated.View>
-        </Pressable>
+        </View>
       </Modal>
     );
   }
diff --git a/src/components/skia/gesture-board.tsx b/src/components/skia/gesture-board.tsx
index b4a278cc2ada84715a4916324b459952ab33c942..4b9b85bedfec21907e498f2fa422fad90655f917 100644
--- a/src/components/skia/gesture-board.tsx
+++ b/src/components/skia/gesture-board.tsx
@@ -2,11 +2,13 @@ import React, {
   useMemo,
   useCallback,
   useState,
+  useEffect,
   forwardRef,
   useRef,
 } from 'react';
 import { View, StyleSheet } from 'react-native';
 import type { ImageSourcePropType } from 'react-native';
+import type { GestureType } from 'react-native-gesture-handler';
 import {
   GestureDetector,
   GestureHandlerRootView,
@@ -38,9 +40,20 @@ import type { EffectParams, EffectTrigger } from '../../types';
 const styles = StyleSheet.create({
   container: {
     aspectRatio: 1,
+    position: 'relative',
   },
 });
 
+const debugChessboard = (event: string, details: Record<string, unknown>) => {
+  const globals = globalThis as unknown as {
+    __CHESSTICIZE_CHESSBOARD_DEBUG__?: boolean;
+    __CHESSTICIZE_CHESSBOARD_DEBUG_SINK__?: (event: string, details: Record<string, unknown>) => void;
+  };
+  if (!globals.__CHESSTICIZE_CHESSBOARD_DEBUG__) return;
+  globals.__CHESSTICIZE_CHESSBOARD_DEBUG_SINK__?.(event, details);
+  console.warn(`[ChessboardDebug] ${event} ${JSON.stringify(details)}`);
+};
+
 interface PromotionInfo {
   from: Square;
   to: Square;
@@ -53,16 +66,51 @@ export interface GestureBoardProps {
   onMove?: (result: MoveResult) => void;
   onIllegalMove?: (from: Square, to: Square) => void;
   renderEffect?: (params: EffectParams) => React.ReactNode;
+  onReady?: () => void;
+  blocksExternalGesture?: React.RefObject<GestureType | undefined>;
+  positionKey?: string;
   spriteSource?: ImageSourcePropType;
 }
 
 export const GestureBoard = forwardRef<ChessboardRef, GestureBoardProps>(
-  ({ onMove, onIllegalMove, renderEffect, spriteSource }, ref) => {
+  (
+    {
+      onMove,
+      onIllegalMove,
+      renderEffect,
+      onReady,
+      blocksExternalGesture,
+      positionKey,
+      spriteSource,
+    },
+    ref
+  ) => {
     const { chess } = useBoardContext();
     const config = useBoardConfig();
     const boardState = useBoardStateValues();
     const { image: spriteImage } = usePieceSpriteSheet(spriteSource);
 
+    useEffect(() => {
+      if (!spriteImage || !onReady) {
+        return;
+      }
+
+      // The first frame lets Skia commit the newly loaded sprite/current FEN.
+      // The second runs only after that paint opportunity, so sibling overlays
+      // cannot become visible while the board still has missing pieces.
+      let secondFrame: number | null = null;
+      const firstFrame = requestAnimationFrame(() => {
+        secondFrame = requestAnimationFrame(onReady);
+      });
+
+      return () => {
+        cancelAnimationFrame(firstFrame);
+        if (secondFrame !== null) {
+          cancelAnimationFrame(secondFrame);
+        }
+      };
+    }, [onReady, positionKey, spriteImage]);
+
     // Effect state for shader effects (all SharedValues for reactivity)
     const effectCenterX = useSharedValue(0);
     const effectCenterY = useSharedValue(0);
@@ -71,30 +119,46 @@ export const GestureBoard = forwardRef<ChessboardRef, GestureBoardProps>(
       'checkmate' | 'check' | 'stalemate' | ''
     >('');
 
-    // Use ref to store promotion info to avoid re-renders during drag
-    // Only the boolean state triggers a render when dialog needs to show/hide
+    // Keep the pending promotion in React state so the dialog render is tied
+    // directly to the pending choice. The ref lets callbacks resolve the move
+    // promise without depending on render timing.
     const promotionInfoRef = useRef<PromotionInfo | null>(null);
-    const [showPromotion, setShowPromotion] = useState(false);
+    const [promotionInfo, setPromotionInfo] = useState<PromotionInfo | null>(null);
+    const showPromotion = promotionInfo !== null;
 
     const handlePromotionRequired = useCallback((info: PromotionInfo) => {
       // If a promotion is somehow already pending, abandon it (resolving its
       // move() promise) before replacing it, so the old one never leaks.
       promotionInfoRef.current?.cancel();
       promotionInfoRef.current = info;
-      setShowPromotion(true);
+      debugChessboard('promotionBoard:required', {
+        from: info.from,
+        to: info.to,
+        color: info.color,
+      });
+      setPromotionInfo(info);
     }, []);
 
     const handlePromotionSelect = useCallback((piece: PieceSymbol) => {
       const info = promotionInfoRef.current;
+      debugChessboard('promotionBoard:select', {
+        piece,
+        hasInfo: !!info,
+      });
       if (info) {
         info.complete(piece);
         promotionInfoRef.current = null;
-        setShowPromotion(false);
+        setPromotionInfo(null);
       }
     }, []);
 
     const handlePromotionCancel = useCallback(() => {
       const info = promotionInfoRef.current;
+      debugChessboard('promotionBoard:cancel', {
+        hasInfo: !!info,
+        from: info?.from,
+        to: info?.to,
+      });
       // The drag gesture animated the pawn into the back-rank target square
       // and raised its zIndex; chess.move() was deferred until the user picked
       // a promotion piece. Cancel means we never execute the move, so snap
@@ -121,9 +185,31 @@ export const GestureBoard = forwardRef<ChessboardRef, GestureBoardProps>(
         info.cancel();
       }
       promotionInfoRef.current = null;
-      setShowPromotion(false);
+      setPromotionInfo(null);
     }, [boardState, config]);
 
+    const handleBoardReset = useCallback(() => {
+      const info = promotionInfoRef.current;
+      debugChessboard('promotionBoard:reset', {
+        hasInfo: !!info,
+        from: info?.from,
+        to: info?.to,
+      });
+      info?.cancel();
+      promotionInfoRef.current = null;
+      setPromotionInfo(null);
+    }, []);
+
+    useEffect(() => {
+      debugChessboard('promotionBoard:state', {
+        showPromotion,
+        hasInfo: !!promotionInfo,
+        refHasInfo: !!promotionInfoRef.current,
+        from: promotionInfo?.from,
+        to: promotionInfo?.to,
+      });
+    }, [promotionInfo, showPromotion]);
+
     // Trigger effect on game events
     const triggerEffect = useCallback(
       (trigger: EffectTrigger, kingColor: 'w' | 'b') => {
@@ -208,6 +294,7 @@ export const GestureBoard = forwardRef<ChessboardRef, GestureBoardProps>(
       boardState,
       moveExecutor,
       defaultHighlightColor: config.colors.lastMoveHighlight,
+      onBeforeReset: handleBoardReset,
     });
 
     const gesture = useBoardGesture({
@@ -217,6 +304,7 @@ export const GestureBoard = forwardRef<ChessboardRef, GestureBoardProps>(
       // Lock the board while the promotion picker is open so a second drag
       // can't start (and re-trigger) another promotion.
       gestureEnabled: config.gestureEnabled && !showPromotion,
+      blocksExternalGesture,
       onIllegalMove,
     });
 
@@ -244,25 +332,27 @@ export const GestureBoard = forwardRef<ChessboardRef, GestureBoardProps>(
 
     return (
       <GestureHandlerRootView style={containerStyle}>
-        <GestureDetector gesture={gesture}>
-          <View style={containerStyle}>
-            <SkiaBoard
+        <View style={containerStyle}>
+          <GestureDetector gesture={gesture}>
+            <View style={containerStyle}>
+              <SkiaBoard
+                config={config}
+                boardState={boardState}
+                spriteImage={spriteImage}
+                renderEffect={renderEffect}
+                effectParams={effectParams}
+              />
+            </View>
+          </GestureDetector>
+          {promotionInfo && (
+            <PromotionDialog
+              color={promotionInfo.color}
+              onSelect={handlePromotionSelect}
+              onCancel={handlePromotionCancel}
               config={config}
-              boardState={boardState}
-              spriteImage={spriteImage}
-              renderEffect={renderEffect}
-              effectParams={effectParams}
             />
-          </View>
-        </GestureDetector>
-        {showPromotion && promotionInfoRef.current && (
-          <PromotionDialog
-            color={promotionInfoRef.current.color}
-            onSelect={handlePromotionSelect}
-            onCancel={handlePromotionCancel}
-            config={config}
-          />
-        )}
+          )}
+        </View>
       </GestureHandlerRootView>
     );
   }
diff --git a/src/components/skia/skia-board.tsx b/src/components/skia/skia-board.tsx
index 759d04e3d374ee79388c2acddc541cfceb0ee7db..c46b6e4e785df632ae3361ac337cefab954d5892 100644
--- a/src/components/skia/skia-board.tsx
+++ b/src/components/skia/skia-board.tsx
@@ -42,12 +42,12 @@ export const SkiaBoard: React.FC<SkiaBoardProps> = React.memo(
       <>
         <BoardBackground config={config} />
         <SkiaHighlights config={config} boardState={boardState} />
-        <SkiaDots config={config} boardState={boardState} />
         <SkiaPiecesAtlas
           spriteImage={spriteImage}
           boardState={boardState}
           pieceSize={pieceSize}
         />
+        <SkiaDots config={config} boardState={boardState} />
       </>
     );
 
diff --git a/src/components/skia/skia-dots.tsx b/src/components/skia/skia-dots.tsx
index 5032bfe81a485ff85d710e7d6e4c0a2e8d78347e..041ff73a79b81185b4719c5e53c6e56ea7a31420 100644
--- a/src/components/skia/skia-dots.tsx
+++ b/src/components/skia/skia-dots.tsx
@@ -9,29 +9,56 @@ interface SkiaDotsProps {
   boardState: BoardState;
 }
 
-// All valid-move dots are built by ONE derived path that reads the shared
-// `validMoves` once (a single subscription) and adds a circle per target —
-// instead of 64 per-square components each subscribing to `validMoves` and
-// scanning it. It re-runs ONLY when `validMoves` changes (not every frame),
-// so it costs nothing at rest. Dots are binary (no per-square opacity), so
-// one Path is a perfect fit.
+// Valid-move markers are built by two derived paths that read the shared
+// `validMoves` once. Empty targets use quiet dots; capture targets use a ring
+// so the marker remains visible when a piece already occupies the target.
 export const SkiaDots: React.FC<SkiaDotsProps> = React.memo(
   ({ config, boardState }) => {
-    const { pieceSize, flipped } = config;
+    const { colors, pieceSize, flipped } = config;
     const radius = pieceSize * 0.15;
+    const captureRadius = pieceSize * 0.38;
     const half = pieceSize / 2;
 
-    const path = useDerivedValue(() => {
-      const p = Skia.Path.Make();
+    const quietPath = useDerivedValue(() => {
+      const path = Skia.Path.Make();
       const moves = boardState.validMoves.get();
       for (let i = 0; i < moves.length; i++) {
-        const pos = squareToPosition(moves[i], pieceSize, flipped);
-        p.addCircle(pos.x + half, pos.y + half, radius);
+        const target = moves[i];
+        if (boardState.squares[target].piece.get()) {
+          continue;
+        }
+        const pos = squareToPosition(target, pieceSize, flipped);
+        path.addCircle(pos.x + half, pos.y + half, radius);
       }
-      return p;
+      return path;
     });
 
-    return <Path path={path} color="rgba(0, 0, 0, 0.3)" opacity={0.5} />;
+    const capturePath = useDerivedValue(() => {
+      const path = Skia.Path.Make();
+      const moves = boardState.validMoves.get();
+      for (let i = 0; i < moves.length; i++) {
+        const target = moves[i];
+        if (!boardState.squares[target].piece.get()) {
+          continue;
+        }
+        const pos = squareToPosition(target, pieceSize, flipped);
+        path.addCircle(pos.x + half, pos.y + half, captureRadius);
+      }
+      return path;
+    });
+
+    return (
+      <>
+        <Path path={quietPath} color={colors.validMoveDot} opacity={0.5} />
+        <Path
+          path={capturePath}
+          color={colors.validMoveCapture}
+          opacity={0.65}
+          style="stroke"
+          strokeWidth={Math.max(2, pieceSize * 0.05)}
+        />
+      </>
+    );
   }
 );
 
diff --git a/src/components/skia/skia-pieces-atlas.tsx b/src/components/skia/skia-pieces-atlas.tsx
index 4cc6f383a6d248943def00bc914cbe572f991f36..95b14879b7ed4f3375aacde96b06a472e59df891 100644
--- a/src/components/skia/skia-pieces-atlas.tsx
+++ b/src/components/skia/skia-pieces-atlas.tsx
@@ -8,6 +8,7 @@ import { SQUARES, BoardState, PieceCode } from '../../state/types';
 // Sprite sheet layout: 6x2 grid (p, n, b, r, q, k for each color)
 // Row 0: white pieces, Row 1: black pieces
 const SPRITE_CELL_SIZE = 128;
+const MAX_PIECE_SLOTS = 32;
 
 // Pre-computed sprite rects for all piece types
 const SPRITE_RECTS: Record<NonNullable<PieceCode>, SkRect> = {
@@ -119,6 +120,14 @@ export const SkiaPiecesAtlas: React.FC<SkiaPiecesAtlasProps> = React.memo(
         );
       }
 
+      // Atlas observes sprites and transforms through separate derived-value
+      // subscriptions. Keep both arrays at the board's fixed maximum piece
+      // count so an update cannot expose different lengths and crash Skia.
+      while (sprites.length < MAX_PIECE_SLOTS) {
+        sprites.push(SPRITE_RECTS.wp);
+        transforms.push(Skia.RSXform(0, 0, -pieceSize, -pieceSize));
+      }
+
       return { sprites, transforms };
     });
 
diff --git a/src/hooks/use-board-gesture.ts b/src/hooks/use-board-gesture.ts
index 92df972f7ec3c671308e8cc0e6cd275469a7144d..8863c68e2567a43612554da49ec7f2d26a8f10ef 100644
--- a/src/hooks/use-board-gesture.ts
+++ b/src/hooks/use-board-gesture.ts
@@ -1,5 +1,7 @@
 import { useMemo, useCallback } from 'react';
+import type { RefObject } from 'react';
 import { Gesture } from 'react-native-gesture-handler';
+import type { GestureType } from 'react-native-gesture-handler';
 import { useSharedValue, withSpring } from 'react-native-reanimated';
 import { scheduleOnRN } from 'react-native-worklets';
 import type { Square } from 'chess.js';
@@ -12,6 +14,7 @@ interface UseBoardGestureProps {
   config: BoardConfig;
   moveExecutor: MoveExecutor;
   gestureEnabled: boolean;
+  blocksExternalGesture?: RefObject<GestureType | undefined>;
   onIllegalMove?: (from: Square, to: Square) => void;
 }
 
@@ -20,9 +23,10 @@ export const useBoardGesture = ({
   config,
   moveExecutor,
   gestureEnabled,
+  blocksExternalGesture,
   onIllegalMove,
 }: UseBoardGestureProps) => {
-  const { pieceSize, animations, flipped } = config;
+  const { pieceSize, animations, flipped, draggableColor } = config;
 
   // Track the currently dragged piece
   const draggedSquare = useSharedValue<Square | null>(null);
@@ -31,13 +35,27 @@ export const useBoardGesture = ({
   // Offset between touch point and piece position (to follow finger exactly)
   const touchOffsetX = useSharedValue(0);
   const touchOffsetY = useSharedValue(0);
+  // Square that was already selected when the current touch began. pan.onBegin
+  // pre-selects the touched piece on touch-down, so the tap handler needs this
+  // to tell a "toggle off" tap from the tap that created the selection.
+  const selectedAtTouchStart = useSharedValue<Square | null>(null);
+  // True while a drag accepted by onStart is in flight. onEnd used to infer
+  // this from the square's zIndex, but a board reset (a fen-prop change
+  // landing mid-drag, e.g. right after an opponent-reply animation) zeroes
+  // zIndex, which made onEnd swallow the drop entirely — no tryMove, no
+  // onIllegalMove, and the sprite was left at the drop point.
+  const dragActivated = useSharedValue(false);
 
   // Stable callback for tryMove
   const handleTryMove = useCallback(
-    (from: Square, to: Square) => {
-      moveExecutor.tryMove(from, to);
+    (from: Square, to: Square, notifyOnInvalid = false) => {
+      void moveExecutor.tryMove(from, to).then((move) => {
+        if (!move && notifyOnInvalid) {
+          onIllegalMove?.(from, to);
+        }
+      });
     },
-    [moveExecutor]
+    [moveExecutor, onIllegalMove]
   );
 
   // Stable callback for selectPiece
@@ -65,19 +83,34 @@ export const useBoardGesture = ({
         // Just prepare for potential drag - store touch info
         const { x, y } = event;
         const square = positionToSquare(x, y, pieceSize, flipped);
+        selectedAtTouchStart.set(boardState.selectedSquare.get());
         const squareState = boardState.squares[square];
         const piece = squareState.piece.get();
+        const turn = boardState.turn.get();
+        const allowedDragColor = draggableColor ?? turn;
 
-        if (piece) {
-          // Store drag start info (but don't start drag yet)
-          draggedSquare.set(square);
-          dragStartX.set(squareState.translateX.get());
-          dragStartY.set(squareState.translateY.get());
+        if (!piece || piece[0] !== allowedDragColor) {
+          draggedSquare.set(null);
+          return;
+        }
+
+        // Store drag start info (but don't start drag yet)
+        draggedSquare.set(square);
+        dragStartX.set(squareState.translateX.get());
+        dragStartY.set(squareState.translateY.get());
+
+        const pieceCenterX = squareState.translateX.get() + pieceSize / 2;
+        const pieceCenterY = squareState.translateY.get() + pieceSize / 2;
+        touchOffsetX.set(x - pieceCenterX);
+        touchOffsetY.set(y - pieceCenterY);
 
-          const pieceCenterX = squareState.translateX.get() + pieceSize / 2;
-          const pieceCenterY = squareState.translateY.get() + pieceSize / 2;
-          touchOffsetX.set(x - pieceCenterX);
-          touchOffsetY.set(y - pieceCenterY);
+        // Start the valid-target calculation before the drag crosses the
+        // min-distance threshold. Otherwise very fast drags can reach onEnd
+        // before the RN-thread select callback has populated validMoves.
+        // Skip when the square is already selected: validMoves are populated,
+        // and re-selecting could race a deselect issued by the tap handler.
+        if (boardState.selectedSquare.get() !== square) {
+          scheduleOnRN(handleSelectPiece, square);
         }
       })
       .onStart(() => {
@@ -89,16 +122,23 @@ export const useBoardGesture = ({
         const squareState = boardState.squares[square];
         const piece = squareState.piece.get();
         const turn = boardState.turn.get();
+        const allowedDragColor = draggableColor ?? turn;
+        const isOwnPiece = piece && piece[0] === allowedDragColor;
+
+        // Do not start dragging opponent pieces; otherwise onUpdate can visibly
+        // move them until onEnd snaps them back.
+        if (!isOwnPiece) {
+          draggedSquare.set(null);
+          return;
+        }
 
         // Raise and scale the piece
+        dragActivated.set(true);
         squareState.zIndex.set(100);
         squareState.scale.set(withSpring(1.1, animations.scale));
 
         // Only show valid moves (dots) for own pieces
-        const isOwnPiece = piece && piece[0] === turn;
-        if (isOwnPiece) {
-          scheduleOnRN(handleSelectPiece, square);
-        }
+        scheduleOnRN(handleSelectPiece, square);
       })
       .onUpdate((event) => {
         'worklet';
@@ -116,15 +156,19 @@ export const useBoardGesture = ({
           event.y - touchOffsetY.get() - pieceSize / 2
         );
       })
-      .onEnd(() => {
+      .onEnd((_event, success) => {
         'worklet';
         const square = draggedSquare.get();
         if (!square) return;
 
         const squareState = boardState.squares[square];
 
-        // Only process drop if piece was actually dragged (zIndex > 0)
-        const wasDragged = squareState.zIndex.get() > 0;
+        // Only process drop if the drag actually activated (onStart accepted
+        // it). Tracked in a gesture-local flag rather than the square's
+        // zIndex: board resets can land mid-drag, wipe zIndex, and must not
+        // swallow the drop.
+        const wasDragged = dragActivated.get();
+        dragActivated.set(false);
         if (!wasDragged) {
           draggedSquare.set(null);
           return;
@@ -132,10 +176,30 @@ export const useBoardGesture = ({
 
         squareState.scale.set(withSpring(1, animations.scale));
 
+        // The gesture was cancelled mid-drag (success === false: e.g. a
+        // board lock flipped `enabled` while the finger was down). Never
+        // derive a drop from wherever the cancellation caught the piece —
+        // snap it home and clear the selection like a failed drop would.
+        if (!success) {
+          const originalPos = squareToPosition(square, pieceSize, flipped);
+          squareState.translateX.set(
+            withSpring(originalPos.x, animations.snapBack)
+          );
+          squareState.translateY.set(
+            withSpring(originalPos.y, animations.snapBack)
+          );
+          squareState.zIndex.set(0);
+          boardState.selectedSquare.set(null);
+          boardState.validMoves.set([]);
+          draggedSquare.set(null);
+          return;
+        }
+
         // Check if this is own piece - only own pieces can make moves
         const piece = squareState.piece.get();
         const turn = boardState.turn.get();
-        const isOwnPiece = piece && piece[0] === turn;
+        const allowedDragColor = draggableColor ?? turn;
+        const isOwnPiece = piece && piece[0] === allowedDragColor;
 
         // If not own piece, just snap back (can't make moves with opponent's pieces)
         if (!isOwnPiece) {
@@ -180,6 +244,18 @@ export const useBoardGesture = ({
           return;
         }
 
+        if (targetSquare !== square && validMoves.length === 0) {
+          const originalPos = squareToPosition(square, pieceSize, flipped);
+          squareState.translateX.set(originalPos.x);
+          squareState.translateY.set(originalPos.y);
+          squareState.zIndex.set(0);
+          boardState.selectedSquare.set(null);
+          boardState.validMoves.set([]);
+          scheduleOnRN(handleTryMove, square, targetSquare, true);
+          draggedSquare.set(null);
+          return;
+        }
+
         // Invalid move - snap back to original position and clear selection
         const originalPos = squareToPosition(square, pieceSize, flipped);
         squareState.translateX.set(
@@ -208,6 +284,7 @@ export const useBoardGesture = ({
             squareState.zIndex.set(0);
           }
         }
+        dragActivated.set(false);
         draggedSquare.set(null);
       });
 
@@ -221,20 +298,30 @@ export const useBoardGesture = ({
         const selectedSquare = boardState.selectedSquare.get();
         const piece = boardState.squares[square].piece.get();
         const turn = boardState.turn.get();
-        const isOwnPiece = piece && piece[0] === turn;
+        const allowedDragColor = draggableColor ?? turn;
+        const isOwnPiece = piece && piece[0] === allowedDragColor;
 
         // Case 1: No piece selected - try to select own piece
         if (!selectedSquare) {
           if (isOwnPiece) {
+            boardState.selectedSquare.set(square);
+            boardState.validMoves.set([]);
             scheduleOnRN(handleSelectPiece, square);
           }
           return;
         }
 
-        // Case 2: Tapped on same piece - deselect
+        // Case 2: Tapped on same piece - deselect, but only when the piece
+        // was already selected before this touch began. pan.onBegin selects
+        // the touched piece on touch-down, so without this guard every quick
+        // tap would read its own selection here and immediately toggle it
+        // off (dots flash and vanish; only >maxDuration presses survived
+        // because the failed tap never ran onEnd).
         if (square === selectedSquare) {
-          boardState.selectedSquare.set(null);
-          boardState.validMoves.set([]);
+          if (selectedAtTouchStart.get() === square) {
+            boardState.selectedSquare.set(null);
+            boardState.validMoves.set([]);
+          }
           return;
         }
 
@@ -247,17 +334,37 @@ export const useBoardGesture = ({
 
         // Case 4: Tapped on another own piece - switch selection
         if (isOwnPiece) {
+          boardState.selectedSquare.set(square);
+          boardState.validMoves.set([]);
           scheduleOnRN(handleSelectPiece, square);
           return;
         }
 
-        // Case 5: Invalid target - deselect
+        // Case 5: Valid moves may not have crossed from the RN thread to the
+        // UI worklet yet after a fast source tap. Let the executor make the
+        // final legality decision so tap-tap stays responsive.
+        if (validMoves.length === 0) {
+          boardState.selectedSquare.set(null);
+          boardState.validMoves.set([]);
+          scheduleOnRN(handleTryMove, selectedSquare, square, true);
+          return;
+        }
+
+        // Case 6: Invalid target - deselect
         boardState.selectedSquare.set(null);
         boardState.validMoves.set([]);
       });
 
-    // Combine gestures - pan takes precedence
-    return Gesture.Simultaneous(tapGesture, panGesture);
+    // Relations attached to a composed Race are not propagated to its raw
+    // gestures. Block the parent scroll from both candidates before composing.
+    if (blocksExternalGesture) {
+      tapGesture.blocksExternalGesture(blocksExternalGesture);
+      panGesture.blocksExternalGesture(blocksExternalGesture);
+    }
+
+    // Tap should not wait for the pan candidate to settle. A real drag still
+    // wins once it crosses minDistance before the tap can activate on release.
+    return Gesture.Race(tapGesture, panGesture);
   }, [
     boardState,
     pieceSize,
@@ -267,8 +374,12 @@ export const useBoardGesture = ({
     dragStartY,
     touchOffsetX,
     touchOffsetY,
+    selectedAtTouchStart,
+    dragActivated,
     animations,
     gestureEnabled,
+    blocksExternalGesture,
+    draggableColor,
     handleTryMove,
     handleSelectPiece,
     handleIllegalMove,
diff --git a/src/hooks/use-chessboard-ref.ts b/src/hooks/use-chessboard-ref.ts
index e1a1653b60bc39a2ac617a9d8b0e0c5c1399dd35..d9040314b24f129923a913a07ca7387114b2432a 100644
--- a/src/hooks/use-chessboard-ref.ts
+++ b/src/hooks/use-chessboard-ref.ts
@@ -22,10 +22,11 @@ export interface ChessboardRef {
   resetBoard: (
     fen?: string,
     opts?: {
-      slide?: { from: Square; to: Square };
+      slide?: { from: Square; to: Square; durationMs?: number };
       lastMove?: { from: Square; to: Square } | null;
+      flipped?: boolean;
     }
-  ) => void;
+  ) => Promise<void>;
   getState: () => ChessboardState;
 }
 
@@ -35,6 +36,7 @@ interface UseChessboardRefProps {
   boardState: BoardState;
   moveExecutor: MoveExecutor;
   defaultHighlightColor?: string;
+  onBeforeReset?: () => void;
 }
 
 export const useChessboardRef = ({
@@ -43,6 +45,7 @@ export const useChessboardRef = ({
   boardState,
   moveExecutor,
   defaultHighlightColor = 'rgba(255, 255, 0, 0.5)',
+  onBeforeReset,
 }: UseChessboardRefProps) => {
   const move = useCallback(
     async (params: {
@@ -88,13 +91,15 @@ export const useChessboardRef = ({
     (
       fen?: string,
       opts?: {
-        slide?: { from: Square; to: Square };
+        slide?: { from: Square; to: Square; durationMs?: number };
         lastMove?: { from: Square; to: Square } | null;
+        flipped?: boolean;
       }
-    ) => {
-      moveExecutor.resetBoard(fen, opts);
+    ): Promise<void> => {
+      onBeforeReset?.();
+      return moveExecutor.resetBoard(fen, opts);
     },
-    [moveExecutor]
+    [moveExecutor, onBeforeReset]
   );
 
   const getState = useCallback((): ChessboardState => {
diff --git a/src/index.tsx b/src/index.tsx
index a50f36cba22769b8a484a1c4429a3135e661dcf3..cfc883fb8cab34b3e13120c18e2dee4f258029ec 100644
--- a/src/index.tsx
+++ b/src/index.tsx
@@ -1,5 +1,6 @@
 import React, { forwardRef } from 'react';
 import type { ImageSourcePropType } from 'react-native';
+import type { GestureType } from 'react-native-gesture-handler';
 import type { Move, Square } from 'chess.js';
 import { BoardStateProvider } from './state';
 import type { BoardStateProviderProps } from './state';
@@ -14,6 +15,13 @@ export interface ChessboardProps
   onMove?: (result: MoveResult) => void;
   onIllegalMove?: (from: Square, to: Square) => void;
   renderEffect?: (params: EffectParams) => React.ReactNode;
+  /**
+   * Called after the current position and piece sprite have had a native frame
+   * to paint. Called again when the `fen` prop changes.
+   */
+  onReady?: () => void;
+  /** Native gesture that the board's tap and pan gestures must block. */
+  blocksExternalGesture?: React.RefObject<GestureType | undefined>;
   /**
    * Optional custom piece sprite sheet. Must match the standard
    * 6×2 / 128px-cell layout (row 0 = white p,n,b,r,q,k; row 1 =
@@ -34,6 +42,7 @@ const Chessboard = forwardRef<ChessboardRef, ChessboardProps>(
       fen,
       boardSize,
       gestureEnabled,
+      draggableColor,
       flipped,
       withLetters,
       withNumbers,
@@ -42,6 +51,8 @@ const Chessboard = forwardRef<ChessboardRef, ChessboardProps>(
       onMove,
       onIllegalMove,
       renderEffect,
+      onReady,
+      blocksExternalGesture,
       spriteSource,
       fontSource,
     },
@@ -52,6 +63,7 @@ const Chessboard = forwardRef<ChessboardRef, ChessboardProps>(
         fen={fen}
         boardSize={boardSize}
         gestureEnabled={gestureEnabled}
+        draggableColor={draggableColor}
         flipped={flipped}
         withLetters={withLetters}
         withNumbers={withNumbers}
@@ -64,6 +76,9 @@ const Chessboard = forwardRef<ChessboardRef, ChessboardProps>(
           onMove={onMove}
           onIllegalMove={onIllegalMove}
           renderEffect={renderEffect}
+          onReady={onReady}
+          blocksExternalGesture={blocksExternalGesture}
+          positionKey={fen}
           spriteSource={spriteSource}
         />
       </BoardStateProvider>
diff --git a/src/state/board-state-context.tsx b/src/state/board-state-context.tsx
index 2f2990b96a62e49047a101e225317ab1a01b20ae..222650a2f245f85e9f15eab121943056cf90668d 100644
--- a/src/state/board-state-context.tsx
+++ b/src/state/board-state-context.tsx
@@ -4,6 +4,7 @@ import type { BoardState, BoardConfig } from './types';
 import { useBoardState } from './use-board-state';
 import { Dimensions } from 'react-native';
 import type { ImageSourcePropType } from 'react-native';
+import type { Color } from 'chess.js';
 import {
   MOVE_SPRING,
   SCALE_SPRING,
@@ -26,6 +27,7 @@ export type BoardStateProviderProps = {
   fen?: string;
   boardSize?: number;
   gestureEnabled?: boolean;
+  draggableColor?: Color | null;
   flipped?: boolean;
   withLetters?: boolean;
   withNumbers?: boolean;
@@ -40,6 +42,8 @@ const defaultColors: BoardConfig['colors'] = {
   lastMoveHighlight: 'rgba(255,255,0, 0.5)',
   checkmateHighlight: '#E84855',
   promotionPieceButton: '#FF9B71',
+  validMoveDot: 'rgba(0, 0, 0, 0.3)',
+  validMoveCapture: 'rgba(0, 0, 0, 0.55)',
 };
 
 const defaultDurations: BoardConfig['durations'] = {
@@ -57,6 +61,7 @@ export const BoardStateProvider: React.FC<BoardStateProviderProps> = ({
   fen,
   boardSize = DEFAULT_BOARD_SIZE,
   gestureEnabled = true,
+  draggableColor = null,
   flipped = false,
   withLetters = true,
   withNumbers = true,
@@ -71,6 +76,7 @@ export const BoardStateProvider: React.FC<BoardStateProviderProps> = ({
       boardSize,
       pieceSize,
       gestureEnabled,
+      draggableColor,
       flipped,
       withLetters,
       withNumbers,
@@ -83,6 +89,7 @@ export const BoardStateProvider: React.FC<BoardStateProviderProps> = ({
       boardSize,
       pieceSize,
       gestureEnabled,
+      draggableColor,
       flipped,
       withLetters,
       withNumbers,
diff --git a/src/state/move-executor.ts b/src/state/move-executor.ts
index 5957db9d36a2d7ab8800c7b171b0c8ef79052d8d..16ee21ed3efa4b8bac663e633a288964f7be2d17 100644
--- a/src/state/move-executor.ts
+++ b/src/state/move-executor.ts
@@ -1,5 +1,5 @@
-import { withSpring } from 'react-native-reanimated';
-import { scheduleOnRN } from 'react-native-worklets';
+import { withSpring, withTiming } from 'react-native-reanimated';
+import { scheduleOnRN, scheduleOnUI } from 'react-native-worklets';
 import type { SharedValue } from 'react-native-reanimated';
 import type { Chess, Move, Square, PieceSymbol } from 'chess.js';
 import type { BoardState, PieceCode } from './types';
@@ -104,59 +104,65 @@ export const createMoveExecutor = (
 
     const fromState = boardState.squares[from];
     const toState = boardState.squares[to];
-    const movingPiece = fromState.piece.get();
-
-    // Handle capture - clear target square piece
-    if (move.captured) {
-      toState.piece.set(null);
-    }
 
     // Animate the piece
     const toPos = squareToPosition(to, pieceSize, flipped);
     const fromPos = squareToPosition(from, pieceSize, flipped);
 
-    // Raise the moving piece
-    fromState.zIndex.set(100);
-
-    // Pre-compute the final piece code to avoid capturing complex objects in worklet
+    // Pre-compute the final piece code to avoid capturing complex objects in
+    // worklet. Derived from the chess move (not the origin sprite slot) so a
+    // sprite-level desync can never plant a null piece on the destination.
     const finalPieceCode: PieceCode = promotionPiece
       ? (`${move.color}${promotionPiece}` as PieceCode)
-      : movingPiece;
+      : (`${move.color}${move.piece}` as PieceCode);
+
+    // Migrate the piece to its destination slot immediately, so the sprite
+    // slots always match the chess position (chess.move already ran above).
+    // Parking the piece in the origin slot until the glide settled — the old
+    // flow — made a fast re-grab of a just-moved piece find an empty
+    // destination square, and any interruption of the glide (board reset,
+    // premove replay) left the sprite stranded on the wrong slot. The glide
+    // renders from the destination slot instead, starting wherever the piece
+    // currently is: the origin square for tap moves, or the drop point for
+    // drags. Setting the destination piece also consumes any captured piece.
+    const startX = fromState.translateX.get();
+    const startY = fromState.translateY.get();
+    fromState.piece.set(null);
+    fromState.translateX.set(fromPos.x);
+    fromState.translateY.set(fromPos.y);
+    fromState.zIndex.set(0);
+    toState.piece.set(finalPieceCode);
+    toState.translateX.set(startX);
+    toState.translateY.set(startY);
+    toState.zIndex.set(100);
 
     const commitMove = (finished?: boolean) => {
       'worklet';
-      // Only commit the sprite writes if the spring actually settled. A
-      // cancelled spring (resetBoard, or a newer animation on the same
-      // square) means whoever cancelled it now owns this square's state —
-      // writing here would smear stale pieces onto the fresh board.
+      // The slots migrated at move time; settling only lowers the piece. A
+      // cancelled glide (resetBoard, a re-grab, or a newer animation on the
+      // same square) means whoever cancelled it owns the square's state now.
       if (finished) {
-        // Move complete - update piece positions
-        toState.piece.set(finalPieceCode);
-        fromState.piece.set(null);
-
-        // Reset position to original square for future use
-        fromState.translateX.set(fromPos.x);
-        fromState.translateY.set(fromPos.y);
-        fromState.zIndex.set(0);
+        toState.zIndex.set(0);
       }
       if (onAnimationComplete) {
         scheduleOnRN(onAnimationComplete);
       }
     };
 
-    // Attach the commit to an axis that actually travels. A spring whose
-    // target equals its current value settles immediately — putting the
-    // callback there commits the move before any motion happens, so the
-    // piece teleports (horizontal moves have dy === 0).
-    const movesVertically = toPos.y !== fromPos.y;
-    fromState.translateX.set(
+    // Attach the commit to the axis with the larger travel. A spring whose
+    // target equals its current value settles immediately — riding a
+    // no-travel axis would fire the commit (and resolve tryMove) before any
+    // motion happens.
+    const movesVertically =
+      Math.abs(toPos.y - startY) >= Math.abs(toPos.x - startX);
+    toState.translateX.set(
       withSpring(
         toPos.x,
         animations.move,
         movesVertically ? undefined : commitMove
       )
     );
-    fromState.translateY.set(
+    toState.translateY.set(
       withSpring(
         toPos.y,
         animations.move,
@@ -174,25 +180,23 @@ export const createMoveExecutor = (
 
       const rookFromState = boardState.squares[rookFrom];
       const rookToState = boardState.squares[rookTo];
-      const rookPiece = rookFromState.piece.get();
+      const rookPiece = `${move.color}r` as PieceCode;
 
       const rookToPos = squareToPosition(rookTo, pieceSize, flipped);
       const rookFromPos = squareToPosition(rookFrom, pieceSize, flipped);
 
-      // The rook slides along its rank — horizontal — so the commit must
-      // ride the X spring (the Y spring settles instantly; see commitMove).
-      rookFromState.translateX.set(
-        withSpring(rookToPos.x, animations.move, (finished) => {
-          'worklet';
-          if (!finished) return;
-          rookToState.piece.set(rookPiece);
-          rookFromState.piece.set(null);
-
-          rookFromState.translateX.set(rookFromPos.x);
-          rookFromState.translateY.set(rookFromPos.y);
-        })
-      );
-      rookFromState.translateY.set(withSpring(rookToPos.y, animations.move));
+      // Same immediate slot migration as the moving piece: the rook lives in
+      // its destination slot from move time and glides in from its origin.
+      const rookStartX = rookFromState.translateX.get();
+      const rookStartY = rookFromState.translateY.get();
+      rookFromState.piece.set(null);
+      rookFromState.translateX.set(rookFromPos.x);
+      rookFromState.translateY.set(rookFromPos.y);
+      rookToState.piece.set(rookPiece);
+      rookToState.translateX.set(rookStartX);
+      rookToState.translateY.set(rookStartY);
+      rookToState.translateX.set(withSpring(rookToPos.x, animations.move));
+      rookToState.translateY.set(withSpring(rookToPos.y, animations.move));
     }
 
     // Handle en passant - remove the captured pawn
@@ -311,19 +315,22 @@ export const createMoveExecutor = (
       // Animate the piece that ends on `to` sliding in from `from` (e.g. when
       // stepping through a game's history). Caller supplies the move; only a
       // single piece is animated.
-      slide?: { from: Square; to: Square };
+      slide?: { from: Square; to: Square; durationMs?: number };
       // From/to squares to highlight as the last move (the move that produced
       // this position). Pass null to clear.
       lastMove?: { from: Square; to: Square } | null;
+      // Target orientation for this position. Supplying it lets a caller
+      // prepare a cross-puzzle position before React publishes new props.
+      flipped?: boolean;
     }
-  ) => {
+  ): Promise<void> => {
     if (fen) {
       try {
         chess.load(fen);
       } catch {
         // Invalid FEN — leave the chess instance untouched and bail out so
         // the board state stays consistent with what's actually on screen.
-        return;
+        return Promise.resolve();
       }
     } else {
       chess.reset();
@@ -331,95 +338,150 @@ export const createMoveExecutor = (
 
     const slide = opts?.slide;
     const lastMove = opts?.lastMove ?? null;
+    const targetFlipped = opts?.flipped ?? flipped;
     const board = chess.board();
+    const squareSnapshots: Array<{
+      piece: PieceCode | null;
+      square: Square;
+      x: number;
+      y: number;
+    }> = [];
 
-    // Update every square. When `slide` is given, the piece landing on
-    // `slide.to` starts at `slide.from` and springs home — so stepping through
-    // a game's history animates the moved piece instead of snapping.
     for (let row = 0; row < 8; row++) {
       for (let col = 0; col < 8; col++) {
         const colChar = String.fromCharCode('a'.charCodeAt(0) + col);
         const rowNum = 8 - row;
         const square = `${colChar}${rowNum}` as Square;
-        const sq = boardState.squares[square];
-
         const piece = board[row][col];
-        sq.piece.set(
-          piece ? (`${piece.color}${piece.type}` as PieceCode) : null
-        );
-        sq.scale.set(1);
-        sq.lastMove.set(
-          !!lastMove && (square === lastMove.from || square === lastMove.to)
-        );
-        sq.inCheck.set(false);
-
-        const pos = squareToPosition(square, pieceSize, flipped);
-        if (slide && square === slide.to) {
-          const fromPos = squareToPosition(slide.from, pieceSize, flipped);
-          sq.zIndex.set(100);
-          sq.translateX.set(fromPos.x);
-          sq.translateY.set(fromPos.y);
-          // zIndex drop rides the axis that actually travels (a no-travel
-          // spring settles instantly — see commitMove in executeMove).
-          const slidesVertically = pos.y !== fromPos.y;
-          const dropZIndex = () => {
-            'worklet';
-            sq.zIndex.set(0);
-          };
-          sq.translateX.set(
-            withSpring(
-              pos.x,
-              animations.move,
-              slidesVertically ? undefined : dropZIndex
-            )
-          );
-          sq.translateY.set(
-            withSpring(
-              pos.y,
-              animations.move,
-              slidesVertically ? dropZIndex : undefined
-            )
-          );
-        } else {
-          sq.translateX.set(pos.x);
-          sq.translateY.set(pos.y);
-          sq.zIndex.set(0);
-        }
+        const pos = squareToPosition(square, pieceSize, targetFlipped);
+        squareSnapshots.push({
+          piece: piece ? (`${piece.color}${piece.type}` as PieceCode) : null,
+          square,
+          x: pos.x,
+          y: pos.y,
+        });
       }
     }
-
-    // Reset other state
-    boardState.turn.set(chess.turn());
-    boardState.selectedSquare.set(null);
-    boardState.validMoves.set([]);
-    boardState.lastMove.set(lastMove);
-
-    // Check / checkmate highlight for the resulting position.
+    const turn = chess.turn();
     const isInCheck = chess.isCheck();
     const kingSquare =
       isInCheck || chess.isCheckmate()
-        ? findKingSquare(chess, chess.turn())
+        ? findKingSquare(chess, turn)
         : null;
-    if (kingSquare) {
-      boardState.squares[kingSquare].inCheck.set(true);
-    }
-    boardState.isCheck.set(isInCheck);
-    boardState.kingInCheckSquare.set(kingSquare);
-
-    // Clear highlights
-    for (const square of Object.keys(boardState.highlights) as Square[]) {
-      boardState.highlights[square].color.set(null);
-    }
+    const slideFromPosition = slide
+      ? squareToPosition(slide.from, pieceSize, targetFlipped)
+      : null;
+
+    // A reset is one visual position change. Run every SharedValue write in a
+    // single UI task so Android cannot paint a partly rewritten board between
+    // individual JS-to-UI writes. The Promise resolves only after that task,
+    // allowing the caller to reveal matching prompt/orientation props.
+    return new Promise<void>((resolve) => {
+      scheduleOnUI(
+        (
+          state: BoardState,
+          snapshots: typeof squareSnapshots,
+          nextTurn: 'w' | 'b',
+          nextLastMove: { from: Square; to: Square } | null,
+          nextKingSquare: Square | null,
+          nextIsInCheck: boolean,
+          nextSlide: typeof slide,
+          nextSlideFromPosition: { x: number; y: number } | null,
+          moveAnimation: BoardConfig['animations']['move'],
+          effects: EffectSharedValues | undefined,
+          onCommitted: () => void
+        ) => {
+          'worklet';
 
-    // Reset shader effect SharedValues so a fresh game's first check or
-    // checkmate doesn't trigger a ripple at the previous game's king
-    // square (centerX/centerY were last written by triggerEffect).
-    if (callbacks.effectSharedValues) {
-      callbacks.effectSharedValues.centerX.set(0);
-      callbacks.effectSharedValues.centerY.set(0);
-      callbacks.effectSharedValues.progress.set(0);
-      callbacks.effectSharedValues.trigger.set('');
-    }
+          for (const snapshot of snapshots) {
+            const sq = state.squares[snapshot.square];
+            sq.piece.set(snapshot.piece);
+            sq.scale.set(1);
+            sq.lastMove.set(
+              !!nextLastMove
+                && (snapshot.square === nextLastMove.from
+                  || snapshot.square === nextLastMove.to)
+            );
+            sq.inCheck.set(snapshot.square === nextKingSquare);
+            if (
+              nextSlide
+              && nextSlideFromPosition
+              && snapshot.square === nextSlide.to
+            ) {
+              const fromPos = nextSlideFromPosition;
+              sq.zIndex.set(100);
+              sq.translateX.set(fromPos.x);
+              sq.translateY.set(fromPos.y);
+              const slidesVertically = snapshot.y !== fromPos.y;
+              const dropZIndex = () => {
+                'worklet';
+                sq.zIndex.set(0);
+              };
+              const timing = nextSlide.durationMs === undefined
+                ? null
+                : { duration: nextSlide.durationMs };
+              sq.translateX.set(
+                timing
+                  ? withTiming(
+                      snapshot.x,
+                      timing,
+                      slidesVertically ? undefined : dropZIndex
+                    )
+                  : withSpring(
+                      snapshot.x,
+                      moveAnimation,
+                      slidesVertically ? undefined : dropZIndex
+                    )
+              );
+              sq.translateY.set(
+                timing
+                  ? withTiming(
+                      snapshot.y,
+                      timing,
+                      slidesVertically ? dropZIndex : undefined
+                    )
+                  : withSpring(
+                      snapshot.y,
+                      moveAnimation,
+                      slidesVertically ? dropZIndex : undefined
+                    )
+              );
+            } else {
+              sq.translateX.set(snapshot.x);
+              sq.translateY.set(snapshot.y);
+              sq.zIndex.set(0);
+            }
+          }
+          state.turn.set(nextTurn);
+          state.selectedSquare.set(null);
+          state.validMoves.set([]);
+          state.lastMove.set(nextLastMove);
+          state.isCheck.set(nextIsInCheck);
+          state.kingInCheckSquare.set(nextKingSquare);
+          for (const square of Object.keys(state.highlights) as Square[]) {
+            state.highlights[square].color.set(null);
+          }
+          if (effects) {
+            effects.centerX.set(0);
+            effects.centerY.set(0);
+            effects.progress.set(0);
+            effects.trigger.set('');
+          }
+          scheduleOnRN(onCommitted);
+        },
+        boardState,
+        squareSnapshots,
+        turn,
+        lastMove,
+        kingSquare,
+        isInCheck,
+        slide,
+        slideFromPosition,
+        animations.move,
+        callbacks.effectSharedValues,
+        resolve
+      );
+    });
   };
 
   const undo = (): Move | null => {
diff --git a/src/state/types.ts b/src/state/types.ts
index 2b02a76df0c9b736fe0c9f1089787dac919ae336..9b7139a692e2bdd344cc08284d36c4ab072fc6a2 100644
--- a/src/state/types.ts
+++ b/src/state/types.ts
@@ -49,6 +49,7 @@ export interface BoardConfig {
   boardSize: number;
   pieceSize: number;
   gestureEnabled: boolean;
+  draggableColor: Color | null;
   flipped: boolean;
   withLetters: boolean;
   withNumbers: boolean;
@@ -58,6 +59,8 @@ export interface BoardConfig {
     lastMoveHighlight: string;
     checkmateHighlight: string;
     promotionPieceButton: string;
+    validMoveDot: string;
+    validMoveCapture: string;
   };
   durations: {
     move: number;
diff --git a/src/state/use-board-state.ts b/src/state/use-board-state.ts
index d5b8613d6bba6d895247254b9497733d4838ba13..db2c18e65c92b6ea3d0af61bbf55250549b440ae 100644
--- a/src/state/use-board-state.ts
+++ b/src/state/use-board-state.ts
@@ -147,8 +147,11 @@ export const useBoardState = (
   // squareStates were created once with the *initial* flipped value, so toggling
   // `flipped` at runtime would otherwise leave un-moved pieces stuck at their
   // original positions. A flip/resize is an orientation/scale change, not a
-  // move, so snap every occupied square to its new logical position instantly
-  // — springing them would make all pieces fly across the board at once.
+  // move, so snap every logical square slot to its new position instantly —
+  // springing them would make all pieces fly across the board at once. Do not
+  // consult the piece SharedValue here: resetBoard writes can still be queued
+  // on Android when this effect runs, so its current value may describe the
+  // previous FEN and cause newly occupied squares to keep the old orientation.
   const isFirstLayoutRef = useRef(true);
   useEffect(() => {
     if (isFirstLayoutRef.current) {
@@ -157,7 +160,6 @@ export const useBoardState = (
     }
     for (const square of SQUARES) {
       const state = squareStates[square];
-      if (state.piece.get() === null) continue;
       const pos = squareToPosition(square, pieceSize, flipped);
       state.translateX.set(pos.x);
       state.translateY.set(pos.y);
@@ -177,6 +179,14 @@ export const useBoardState = (
       isFirstFenRef.current = false;
       return;
     }
+    // The screen aligns the fen prop with a position the board already
+    // reached through animated moves (opponent replies, premove replays).
+    // When the position already matches, the rewrite below would be a no-op
+    // for the pieces but would still wipe live gesture state — selection,
+    // valid-move dots, and an in-flight drag (zIndex) — mid-gesture. Skip it.
+    if (initialFen && chess.fen() === initialFen) {
+      return;
+    }
     if (!initialFen) {
       chess.reset();
     } else {
