buildscript {
    ext {
        buildToolsVersion = property("chessticizeBuildTools").toString()
        minSdkVersion = property("chessticizeMinSdk").toString().toInteger()
        compileSdkVersion = property("chessticizeCompileSdk").toString().toInteger()
        targetSdkVersion = property("chessticizeTargetSdk").toString().toInteger()
        ndkVersion = property("chessticizeNdk").toString()
        kotlinVersion = "2.1.20"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"

allprojects {
    repositories {
        google()
        mavenCentral()
        maven {
            url("$rootDir/../node_modules/detox/Detox-android")
        }
    }
}

// Some native React Native libraries rely on the Android Gradle Plugin's NDK
// default instead of the root React Native convention. Keep every compiled
// library on the repository's canonical, installed NDK so release builds do
// not silently select an unpinned side-by-side version.
subprojects { subproject ->
    subproject.pluginManager.withPlugin("com.android.library") {
        subproject.android.ndkVersion = rootProject.ext.ndkVersion
    }
}
